# 実機テスト手順

自動テストでは確かめられない項目をまとめる（→ [plan.md](plan.md) 16-3）。
**実際の端末・QRリーダー・プリンタが要る。** 導入前に上から順に実施する。

## 準備

| 用意するもの | 備考 |
|---|---|
| Windows 端末 | 実際に設置する機種。タッチ操作ができること |
| QRリーダー | シリアル（USB仮想COM）接続のもの |
| プリンタ | QRカードを名刺サイズで印刷できること |
| テスト用スタッフ | 2名以上（在職中・退職済を作る） |

**結果はこのファイルに直接書き込まず、印刷するかコピーして使う。**
実施日・実施者・端末名を控えること。

ログの場所: `%LOCALAPPDATA%\QTimeRecord\logs\qtimerecord-YYYYMMDD.log`

---

## 16-3-1. QRリーダー実機での読み取り

発行した QR が、画面表示・印刷の両方で読めることを確かめる。

1. 管理画面 → スタッフ管理 → 新規スタッフ登録（QRが自動発行される）
2. 「画像を保存」で PNG を保存する
3. **PNGを画面に表示した状態**でリーダーにかざす
4. 待機画面へ戻り、**印刷したカード**を同じようにかざす

| 確認 | 期待 |
|---|---|
| □ 画面表示のQR | 打刻種別の選択画面へ進む |
| □ 印刷したカード | 同上 |
| □ 読み取り距離 | 実際の設置位置（10〜20cm程度）で読める |
| □ ログ | `QR を読み取りました: <8文字>` が残る。**トークン本体が残っていないこと** |

読めない場合はカードの余白（QRの周囲）を確認する。QRは静音領域が足りないと読めない。

---

## 16-3-2. シリアル設定

1. 管理画面 → 店舗設定 → 「再検出」でCOMポート一覧が出る
2. 正しいポートを選び「接続テストを実施」→ QRをかざす
3. **ボーレートをわざと変える**（9600 → 19200 など）→ 再度接続テスト

| 確認 | 期待 |
|---|---|
| □ ポート検出 | 接続しているリーダーのCOM番号が一覧に出る |
| □ 接続テスト成功 | 10秒以内に読み取ると成功と表示される |
| □ ボーレート違い | 読み取れず、失敗として表示される（**成功と出ないこと**） |
| □ 存在しないポート | 「ポートを開けませんでした」等の理由が出る |
| □ 他アプリが使用中 | 別のシリアル通信ソフトでポートを開いた状態で試す。理由が出ること |

---

## 16-3-3. 接続断

**動作中にUSBを抜く。** これは日常的に起こる（掃除・配線の引っかかり）。

1. 待機画面を表示した状態で、リーダーのUSBを抜く
2. 5秒ほど待つ

| 確認 | 期待 |
|---|---|
| □ 待機画面 | ピルが赤の「リーダー未接続」に変わる |
| □ 待機画面 | 「接続が切れました。再接続しています。」が出る |
| □ 打刻画面 | 打刻中に抜いた場合、4つの打刻ボタンが押せなくなり、キャンセルだけ残る |
| □ アプリ | **落ちないこと** |
| □ ログ | `QR リーダーとの接続が切れました。` が残る |

---

## 16-3-4. 再接続

1. 16-3-3 の状態でUSBを挿し直す
2. 最大30秒待つ（再接続は 1秒 → 2秒 → 5秒 → 10秒 → 30秒 の間隔で試みる）

| 確認 | 期待 |
|---|---|
| □ 待機画面 | ピルが緑の「リーダー待機中」に戻る |
| □ 打刻 | かざすと打刻できる |
| □ ログ | `QR リーダーに接続しました` が残る |
| □ アプリの再起動が不要であること | |

---

## 16-3-5. タッチ操作

**指で操作する。** マウスでは誤タップの起きやすさが分からない。

| 確認 | 期待 |
|---|---|
| □ 打刻の4ボタン | 指で確実に押せる。隣を誤って押さない |
| □ キャンセル | 打刻ボタンから十分離れている |
| □ テンキー（管理者ログイン） | 隣の数字を誤って押さない |
| □ 打刻の修正ダイアログ | 削除ボタンを保存と間違えて押さない |
| □ スクロール | スタッフ管理の右側・店舗設定が指でスクロールできる |
| □ 手袋 | 調理・清掃で手袋を使う店舗の場合、手袋のまま押せるか |

押しにくい箇所があれば、**どのボタンがどう押しにくいか**を具体的に記録する。

---

## 16-3-6. 長時間連続稼働

**営業時間より長く動かす。** メモリの増加は短時間では出ない。

1. 待機画面のまま8時間以上放置する（時計が動き続ける状態）
2. 途中で数回、打刻を行う

| 確認 | 期待 |
|---|---|
| □ 8時間後の打刻 | 通常どおり打刻できる |
| □ メモリ | タスクマネージャーで増え続けていない（開始時と比べて大きく増えていない） |
| □ 時計 | 待機画面の時計がずれていない |
| □ ログ | 同じ警告が大量に繰り返されていない |
| □ ログファイル | 日付が変わるとファイルが分かれている |

開始時と終了時のメモリ使用量を記録すること。

---

## 16-3-7. 電源断

**打刻が消えないことの最終確認。** 開発中に `Environment.FailFast` による強制終了では
残ることを確認済みだが、OS のキャッシュを含む経路は実機でしか確かめられない。

1. 打刻する（完了画面が出るまで待つ）
2. **完了画面が出た直後に、電源ケーブルを抜く**（シャットダウンしない）
3. 電源を入れ直してアプリを起動する
4. 管理画面 → 勤務状況 で、その打刻が残っているか確認する

| 確認 | 期待 |
|---|---|
| □ 打刻の残存 | 手順1の打刻が一覧に出る |
| □ 起動 | エラーなく起動する |
| □ DBの破損 | 他の打刻も消えていない |

**残っていなければ導入を止める。** 勤怠の証跡が失われる状態で運用できない。

---

## 16-3-8. 画面解像度

設置する端末の解像度で確認する。開発時は 1366x768 を基準にしている。

| 画面 | 1366x768 | 1920x1080 |
|---|---|---|
| □ 待機画面 | お知らせが隠れない | |
| □ 打刻種別の選択 | 4ボタンとキャンセルが出る | |
| □ 打刻完了 | ボタン・注意書きが切れない | |
| □ 管理者ログイン | ログイン・キャンセルが押せる | |
| □ 勤務状況 | 一覧の右端（操作列）まで見える | |
| □ スタッフ管理 | QRカードのプレビューが見える | |
| □ 店舗設定 | 保存・破棄が押せる | |
| □ 打刻の修正ダイアログ | 保存・削除が画面内に収まる | |

**切れている箇所があれば、画面名と解像度を記録する。**

---

## 実施記録

| 項目 | 結果 | 実施日 | 備考 |
|---|---|---|---|
| 16-3-1 QR読み取り | | | |
| 16-3-2 シリアル設定 | | | |
| 16-3-3 接続断 | | | |
| 16-3-4 再接続 | | | |
| 16-3-5 タッチ操作 | | | |
| 16-3-6 長時間稼働 | | | |
| 16-3-7 電源断 | | | |
| 16-3-8 画面解像度 | | | |
