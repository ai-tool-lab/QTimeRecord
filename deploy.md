# QTimeRecord 導入手順書

店舗のパソコンに QTimeRecord を入れて、使える状態にするまでの手順です。
**上から順に、そのとおりに進めれば終わります。** 飛ばさないでください。

対象OS: **Windows 11**

---

## 目次

| 章 | やること | だいたいの時間 |
|---|---|---|
| [0](#0-はじめに) | 用意するものを確認する | 5分 |
| [1](#1-アプリを作る開発用パソコンで行う) | アプリを作る（開発用パソコン） | 10分 |
| [2](#2-店舗のパソコンへコピーする) | 店舗のパソコンへコピーする | 10分 |
| [3](#3-qrリーダーをつなぐ) | QRリーダーをつなぐ | 10分 |
| [4](#4-最初の設定をする) | 最初の設定をする | 10分 |
| [5](#5-qrリーダーの設定と接続テスト) | QRリーダーの設定と接続テスト | 10分 |
| [6](#6-スタッフを登録してqrカードを配る) | スタッフを登録してQRカードを配る | 人数×3分 |
| [7](#7-パソコンの電源が切れないようにする) | 電源が切れないようにする | 5分 |
| [8](#8-自動で立ち上がるようにする) | 自動で立ち上がるようにする | 5分 |
| [9](#9-バックアップのしかた) | バックアップのしかた | 5分 |
| [10](#10-バージョンアップのしかた) | バージョンアップのしかた | — |
| [11](#11-困ったときは) | 困ったときは | — |

---

## 0. はじめに

### 用意するもの

| もの | 説明 |
|---|---|
| **店舗に置くパソコン** | Windows 11。タッチで操作できるものが望ましい |
| **QRリーダー** | USBでつなぐタイプ。「シリアル通信（COMポート）」に対応しているもの |
| **USBメモリ** | アプリを運ぶのに使う。**1GB以上** の空きがあれば足りる |
| **プリンタ** | スタッフに配るQRカードを印刷する。無くても後回しにできる |
| **開発用パソコン** | アプリを作る作業に使う。店舗のパソコンとは別でよい |

### 知っておいてほしいこと

- **インターネットにつながっていなくても動きます。** 店舗のパソコンをネットにつなぐ必要はありません
- **打刻のデータは店舗のパソコンの中だけに保存されます。** だから[バックアップ](#9-バックアップのしかた)が大切です
- **管理者パスワード（8桁の数字）を忘れると、管理画面に入れなくなります。** 紙に書いて、店長が保管してください

---

## 1. アプリを作る（開発用パソコンで行う）

店舗のパソコンにコピーする「アプリの本体」を作ります。

### 1-1. 作る

開発用パソコンで、QTimeRecord のフォルダを開いて、次のコマンドを実行します。

```bash
dotnet publish QTimeRecord.App -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:EnableCompressionInSingleFile=true -o publish
```

> **このコマンドの意味**
> `--self-contained true` は「アプリが動くのに必要なものを全部いっしょに詰め込む」という指定です。
> こうしておくと、**店舗のパソコンに何もインストールしなくても動きます。**
> `-p:PublishSingleFile=true` は「1つのファイルにまとめる」という指定です。ファイルが散らからず、コピーが簡単になります。

3〜5分ほどかかります。最後に次のように出れば成功です。

```
QTimeRecord.App -> I:\Project\QTimeRecord\publish\
```

### 1-2. できたものを確認する

`publish` フォルダを開くと、こうなっています。

| ファイル | 説明 |
|---|---|
| `QTimeRecord.App.exe` | **アプリ本体。** 約66MB |
| `appsettings.json` | データの保存場所などの設定。**触らないでください** |
| `THIRD-PARTY-NOTICES.txt` | 使っている部品のライセンス表示。**必ず一緒に配ってください**（法律上の決まりです） |
| `QTimeRecord.App.pdb` / `QTimeRecord.Core.pdb` | 不具合を調べるときに使うファイル。消しても動きますが、残しておくと原因が分かりやすくなります |

**うまくいかないとき** → [11-1](#11-1-1章でエラーが出る)

---

## 2. 店舗のパソコンへコピーする

### 2-1. USBメモリに入れる

1. 開発用パソコンにUSBメモリをさす
2. `publish` フォルダを**まるごと**USBメモリにコピーする
3. USBメモリを安全に取り外す

### 2-2. 店舗のパソコンに置く

1. 店舗のパソコンにUSBメモリをさす
2. エクスプローラー（フォルダのアイコン）を開く
3. 左側の **「PC」** をクリック → **「ローカルディスク (C:)」** をダブルクリック
4. 何もないところで**右クリック** → **「新規作成」** → **「フォルダー」**
5. 名前を **`QTimeRecord`** にする（半角英字。スペースを入れない）

> 「この操作を行うには管理者の権限が必要です」と出たら **「続行」** を押してください。
> パスワードを聞かれたら、パソコンの管理者パスワードを入れます。

6. USBメモリの `publish` フォルダの**中身をすべて**、いま作った `C:\QTimeRecord` にコピーする

コピーが終わると、こうなっているはずです。

```
C:\QTimeRecord\
    QTimeRecord.App.exe
    appsettings.json
    THIRD-PARTY-NOTICES.txt
    （.pdb ファイル2つ）
```

### 2-3. 動くか確かめる

`QTimeRecord.App.exe` を**ダブルクリック**します。

- 青い画面が出て、**「初回セットアップ」**と表示されれば成功です
- そのまま次の章へ進みます（まだ何も入力しないでください）

> **「WindowsによってPCが保護されました」** と出たとき
> 「詳細情報」をクリック → **「実行」** を押してください。
> インターネットからダウンロードしたファイルではないため、無視して問題ありません。

**うまくいかないとき** → [11-2](#11-2-アプリが立ち上がらない)

---

## 3. QRリーダーをつなぐ

### 3-1. さす

QRリーダーのUSBを店舗のパソコンにさします。
「デバイスをセットアップしています」と出たら、終わるまで待ちます（1〜2分）。

### 3-2. COMポート番号を調べる

QRリーダーには **「COM3」「COM5」** のような番号が割り当てられます。
あとで設定に使うので、**メモしてください。**

1. キーボードの **Windowsキー** を押しながら **X** を押す
2. 出てきたメニューから **「デバイス マネージャー」** をクリック
3. 一覧の中の **「ポート (COM と LPT)」** の左の **>** をクリックして開く
4. QRリーダーの名前（例: `USB-SERIAL CH340 (COM3)`）が出る
5. **カッコの中の COM◯◯ をメモする**

> **「ポート (COM と LPT)」が無いとき**
> QRリーダーのドライバーが入っていません。
> リーダーに付属のCD、またはメーカーのサイトからドライバーを入れてください。
> それでも出ない場合、そのリーダーは**シリアル通信に対応していない**可能性があります
> （キーボードとして動くタイプのリーダーは、このアプリでは使えません）。

**うまくいかないとき** → [11-3](#11-3-qrリーダーが見つからない)

---

## 4. 最初の設定をする

アプリを立ち上げると、**「初回セットアップ」**の画面が出ます。

### 4-1. 入力する

| 項目 | 何を入れるか | 例 |
|---|---|---|
| 会社名 | 会社の正式名称 | 株式会社クオリティフーズ |
| 店舗名 | この店の名前 | 相模原店 |
| 店舗コード（任意） | 店の番号。無ければ空でよい | 101 |
| **1日の開始時刻** | **下の説明を読んでから決める** | 09:00 |
| **1日の終了時刻** | 同上 | 22:00 |
| 管理者パスワード | **8桁の数字** | 12345678 |

### 4-2. 「1日の開始時刻」がとても大切です

これは **「何時から何時までを1日とみなすか」** の設定です。

**深夜まで営業する店では、これを正しく設定しないと勤怠がずれます。**

> **例: 11:00 開店・翌 5:00 閉店の居酒屋の場合**
>
> 開始時刻を **11:00** にします。
> こうすると、**夜10時に出勤して深夜2時に退勤した人**は、
> 「1日にまたがる2日ぶんの勤務」ではなく、**出勤した日の1回の勤務**として記録されます。
>
> もし開始時刻を 0:00 のままにすると、同じ勤務が2日に分かれてしまい、
> 給与計算のときに直す手間が発生します。

| 店のタイプ | 開始時刻の目安 |
|---|---|
| 朝から夕方まで（事務所・工場） | `00:00`（そのままでよい） |
| 昼から夜まで（飲食店・小売） | 開店時刻（例 `11:00`） |
| 深夜まで営業（居酒屋・バー） | 開店時刻（例 `17:00`） |
| 24時間営業 | 開始と終了を**同じ時刻**にする（例 両方 `00:00`） |

**あとから変更できます**が、**変更しても過去の記録は書き換わりません**（それが正しい動きです）。
最初に決めておくのがいちばん楽です。

### 4-3. 管理者パスワードを決める

- **8桁の数字**です。数字以外は使えません
- **忘れると管理画面に入れなくなります。** 紙に書いて店長が保管してください
- 誕生日や `12345678` のような分かりやすい数字は避けてください

### 4-4. 保存する

すべて入力したら **「登録して開始する」** を押します。
待機画面（大きな時計が出る画面）に変われば完了です。

---

## 5. QRリーダーの設定と接続テスト

### 5-1. 管理画面に入る

1. 待機画面の右上 **「管理画面ログイン」** を押す
2. テンキーで**管理者パスワード（8桁）**を入れる
3. **「ログイン」** を押す

### 5-2. リーダーの設定をする

1. 上の **「店舗設定」** タブを押す
2. 真ん中の **「リーダー設定」** を見る
3. **「再検出」** を押す
4. **「接続ポート (COM)」** をタップして、[3-2でメモしたCOM番号](#3-2-comポート番号を調べる)を選ぶ
5. ほかの項目（ボーレートなど）は、**QRリーダーの説明書に書いてある値**に合わせる
   - 説明書が無ければ、まずはそのまま（9600 / 8 / None / One）で試す

### 5-3. 接続テストをする

1. **「接続テストを実施」** を押す
2. 「QRコードをかざしてください」と出たら、**手元にあるQRコード**をリーダーにかざす
   - まだスタッフのQRを作っていないので、**商品のバーコードやスマホのQRコードでかまいません**
3. **緑色で成功のメッセージが出れば成功です**

| 出たもの | 意味と対処 |
|---|---|
| 緑で「読み取りを確認しました」 | 成功。次へ進む |
| 「ポートを開けませんでした」 | COM番号が違う。[3-2](#3-2-comポート番号を調べる)からやり直す |
| 10秒たっても何も起きない | ボーレートが違う可能性が高い。リーダーの説明書を確認する |

### 5-4. 保存する

**画面右下の「設定を保存する」を必ず押してください。**
押さないと設定が消えます（押し忘れると赤い字で警告が出ます）。

---

## 6. スタッフを登録してQRカードを配る

### 6-1. スタッフを登録する

1. 上の **「スタッフ管理」** タブを押す
2. 右上の **「新規スタッフ登録」** を押す
3. 入力する

| 項目 | 説明 |
|---|---|
| 氏名 | **必須** |
| フリガナ | カタカナで。名簿の並び順に使います |
| 社員番号 | 給与ソフトと突き合わせるときに使う。**同じ番号は使えません** |
| 区分 | 社員 / アルバイト / パート から選ぶ |

4. **「保存」** を押す

> **登録すると同時にQRコードが自動で作られます。** 別の作業は要りません。

### 6-2. QRカードを保存する

1. 一覧で、いま登録した人をタップする
2. 右側にQRカードのプレビューが出る
3. **「画像を保存」** を押す
4. 保存先を選んで保存する（そのままでよければ「保存」を押すだけ）

### 6-3. 印刷して渡す

1. 保存した画像ファイルをダブルクリックで開く
2. 右クリック → **「印刷」**
3. **名刺サイズ（91×55mm）**で印刷する

> **印刷のコツ**
> QRコードの**まわりの白い余白を切り取らないでください。**
> 余白が足りないとリーダーが読めなくなります。

4. **印刷したカードを実際にリーダーにかざして、読めるか確認してから渡してください**

### 6-4. 打刻を試す

1. 右上の **「ログアウト」** を押して待機画面に戻る
2. 印刷したカードをかざす
3. 打刻の種類を選ぶ画面が出れば成功です
4. **「出勤」**を押して、完了画面が出ることを確認する

---

## 7. パソコンの電源が切れないようにする

**スリープしたパソコンでは打刻できません。** 必ず設定してください。

1. **Windowsキー** を押して **「設定」** と入力し、設定を開く
2. 左の **「システム」** → **「電源とバッテリー」**
3. **「画面とスリープ」** を開く
4. すべての項目を **「なし」** にする

| 項目 | 設定 |
|---|---|
| 電源接続時に、次の時間が経過した後に画面をオフにする | **なし** |
| 電源接続時に、次の時間が経過した後にデバイスをスリープ状態にする | **なし** |

> ノートパソコンの場合、**「バッテリー駆動時」**の項目も同じようにしてください。

### Windows Update で勝手に再起動しないようにする

1. 設定 → **「Windows Update」** → **「詳細オプション」**
2. **「アクティブ時間」** を開く
3. **営業時間を含む時間帯**を設定する（例: 8時〜23時）

これで、営業中に勝手に再起動されることを防げます。

---

## 8. 自動で立ち上がるようにする

パソコンの電源を入れたら、自動でQTimeRecordが立ち上がるようにします。

1. `C:\QTimeRecord` フォルダを開く
2. `QTimeRecord.App.exe` を**右クリック** → **「その他のオプションを確認」** → **「ショートカットの作成」**
   - デスクトップに作るか聞かれたら「はい」
3. できたショートカットを**右クリック** → **「切り取り」**
4. **Windowsキー** を押しながら **R** を押す
5. 出てきた窓に **`shell:startup`** と入力して **Enter**
6. 開いたフォルダの中で**右クリック** → **「貼り付け」**

### 確認する

1. パソコンを**再起動**する
2. サインインしたあと、QTimeRecordが自動で立ち上がれば成功です

---

## 9. バックアップのしかた

**打刻のデータは店舗のパソコンの中だけにあります。**
パソコンが壊れると、勤怠の記録が失われます。**月に1回はバックアップしてください。**

### 9-1. データがある場所

```
C:\Users\（ユーザー名）\AppData\Local\QTimeRecord\
```

このフォルダは通常は隠れています。次の方法で開けます。

1. **Windowsキー** を押しながら **R** を押す
2. **`%LOCALAPPDATA%\QTimeRecord`** と入力して **Enter**

### 9-2. バックアップの手順

1. **QTimeRecord を終了する**（右上の×を押す）
   - **アプリを開いたままコピーしないでください。** 途中の状態がコピーされることがあります
2. 上の方法で `QTimeRecord` フォルダを開く
3. **フォルダをまるごと** USBメモリや別のパソコンにコピーする
4. コピーしたフォルダの名前を **`QTimeRecord_2026-09-07`** のように日付付きに変える

> **`qtimerecord.db` だけをコピーしないでください。**
> 同じフォルダにある `-wal` `-shm` という2つのファイルにも、
> 最新の打刻が入っていることがあります。**フォルダごとコピーしてください。**

### 9-3. 元に戻すとき

1. QTimeRecord を終了する
2. `%LOCALAPPDATA%\QTimeRecord` のフォルダの中身を消す
3. バックアップしたフォルダの中身をコピーする
4. QTimeRecord を立ち上げる

---

## 10. バージョンアップのしかた

新しいバージョンに入れ替えるときの手順です。

1. **[9章](#9-バックアップのしかた)のバックアップを取る**（必ず先に行ってください）
2. QTimeRecord を終了する
3. 新しい `publish` フォルダの中身を、`C:\QTimeRecord` に**上書きコピー**する
4. QTimeRecord を立ち上げる

> **打刻のデータは消えません。**
> データは `C:\QTimeRecord`（アプリ）ではなく `%LOCALAPPDATA%\QTimeRecord`（データ）にあるため、
> アプリを入れ替えてもデータはそのまま残ります。
>
> データの保存形式が変わるバージョンアップでは、**立ち上げたときに自動で変換されます。**
> 変換に失敗した場合はエラーが出て起動しません。そのときは[11-4](#11-4-バージョンアップ後に起動しない)を見てください。

---

## 11. 困ったときは

### まず見るところ: ログ

アプリが何をしたかは、**ログファイル**に記録されています。

1. **Windowsキー** を押しながら **R** を押す
2. **`%LOCALAPPDATA%\QTimeRecord\logs`** と入力して **Enter**
3. いちばん新しい日付のファイル（`qtimerecord-20260907.log`）をメモ帳で開く
4. 一番下のあたりに、エラーの内容が書かれています

問い合わせるときは、**このファイルを添えてください。**

---

### 11-1. 1章でエラーが出る

| エラーの内容 | 対処 |
|---|---|
| `dotnet: コマンドが見つかりません` | .NET SDK が入っていません。https://dotnet.microsoft.com/download から **.NET 10 SDK** を入れてください |
| `NETSDK1045` などバージョンのエラー | .NET SDK のバージョンが古いです。**10.0以上**を入れてください |
| ファイルが使用中でコピーできない | QTimeRecord が起動したままです。終了してからやり直してください |

---

### 11-2. アプリが立ち上がらない

| 症状 | 原因と対処 |
|---|---|
| ダブルクリックしても何も起きない | 30秒ほど待ってください。初回は時間がかかります |
| 「WindowsによってPCが保護されました」 | 「詳細情報」→「実行」を押す |
| 「起動できません／データベースを初期化できませんでした」 | データの保存先に書き込めていません。[ログ](#まず見るところ-ログ)を確認してください |
| 一瞬出てすぐ消える | `appsettings.json` が無い可能性があります。[2-2](#2-2-店舗のパソコンに置く)のコピーをやり直してください |

---

### 11-3. QRリーダーが見つからない

1. **USBを抜いて、さし直す**（別のUSB口も試す）
2. デバイスマネージャーの **「ポート (COM と LPT)」** に出るか確認する
3. 出ない場合:
   - **ドライバーが入っていない** → メーカーのサイトからドライバーを入れる
   - **「ほかのデバイス」に「!」付きで出ている** → 同じくドライバーが必要
   - **どこにも出ない** → そのリーダーはシリアル通信に対応していない可能性があります

> **キーボードのように文字を打ち込むタイプのQRリーダーは、このアプリでは使えません。**
> 購入前に「**シリアル通信（COMポート）対応**」と書かれているか確認してください。

---

### 11-4. バージョンアップ後に起動しない

1. [ログ](#まず見るところ-ログ)を開いて、`データベースを初期化できませんでした` が無いか見る
2. あった場合、データの形式変換に失敗しています
3. **[9-3](#9-3-元に戻すとき)の手順でバックアップから戻し**、古いバージョンのアプリに戻してください
4. ログを添えて開発元へ連絡してください

---

### 11-5. かざしても反応しない

| 確認すること | 対処 |
|---|---|
| 画面右上のランプが**赤（リーダー未接続）**になっていないか | USBを抜きさしする。30秒ほどで自動的に戻ります |
| ほかの人のカードは読めるか | その人のカードだけの問題なら、[QRを再発行](#6-2-qrカードを保存する)する |
| カードの余白が切れていないか | QRのまわりの白い部分を含めて印刷し直す |
| 印刷がかすれていないか | インクを替えて印刷し直す |

---

### 11-6. 「登録されていないQRコードです」と出る

- **QRを再発行したあとに、古いカードを使っている**可能性がいちばん高いです
- 管理画面 → スタッフ管理 → その人を選び、**新しいカードを印刷して渡してください**
- 古いカードは**回収して捨ててください**（もう使えません）

---

### 11-7. 管理者パスワードを忘れた

**残念ながら、パスワードを調べる方法はありません。**（安全のため、そういう作り方をしています）

次のどちらかになります。

- **打刻のデータを残したまま直したい** → 開発元へ連絡してください
- **最初からやり直してよい** →
  1. QTimeRecord を終了する
  2. **[9章](#9-バックアップのしかた)のバックアップを取る**（念のため）
  3. `%LOCALAPPDATA%\QTimeRecord` の中の `qtimerecord.db` `qtimerecord.db-wal` `qtimerecord.db-shm` を消す
  4. QTimeRecord を立ち上げると、[4章](#4-最初の設定をする)の初回セットアップからやり直せます
  5. **これまでの打刻とスタッフの情報はすべて消えます**

---

### 11-8. 5回間違えてロックされた

**5分待てば自動的に解除されます。** 画面に残り時間が出ています。
アプリを再起動しても解除されません（そういう作り方をしています）。

---

## 導入完了チェックリスト

すべてにチェックが付けば導入完了です。

- [ ] `C:\QTimeRecord` にアプリがコピーされている
- [ ] `THIRD-PARTY-NOTICES.txt` が一緒に置かれている
- [ ] 初回セットアップが終わり、待機画面が出る
- [ ] **1日の開始時刻が、店の営業形態に合っている**
- [ ] 管理者パスワードを紙に書いて店長が保管している
- [ ] QRリーダーの接続テストが成功している
- [ ] スタッフ全員のQRカードを印刷して配った
- [ ] **印刷したカードで実際に打刻できた**
- [ ] スリープしない設定になっている
- [ ] Windows Update のアクティブ時間を設定した
- [ ] 自動起動の設定をして、再起動して確認した
- [ ] バックアップの手順を店長に説明した

> 導入後、**[実機テスト手順.md](実機テスト手順.md)** の項目も実施してください。
> 特に **16-3-7（電源断のテスト）** は、勤怠データが失われないことの最終確認です。
