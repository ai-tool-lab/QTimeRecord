# QTimeRecord 実装計画

小規模事業所向けデジタルタイムカードソフト **QTimeRecord** の MVP 実装計画。

> このドキュメントは実装の進行管理を兼ねる。工程が完了したらチェックボックスを `[x]` に更新する。
> 実装を始める前に、**「2. 確認事項」の【高】項目だけは発注者に確認する**こと。
> ここが未確定のまま進むと、DB スキーマとCSV仕様の作り直しになる。

**参照資料**

| 資料 | 内容 |
|---|---|
| [DESIGN.md](DESIGN.md) | 配色・タイポグラフィ・余白・コンポーネント定義（Precision Kiosk Blue） |
| [img/メイン画面.png](img/メイン画面.png) / .html | 待機画面 |
| [img/メイン画面_打刻.png](img/メイン画面_打刻.png) / .html | 打刻種別選択・打刻完了 |
| [img/管理者ログイン.png](img/管理者ログイン.png) / .html | 管理者認証 |
| [img/管理者_勤務状況.png](img/管理者_勤務状況.png) / .html | 月次打刻一覧・CSV出力 |
| [img/管理者_スタッフ管理.png](img/管理者_スタッフ管理.png) / .html | スタッフ名簿・QRカード発行 |
| [img/管理者_店舗設定.png](img/管理者_店舗設定.png) / .html | 会社情報・お知らせ・シリアル設定 |
| [prompt.txt](prompt.txt) | 元の要件定義 |

---

## 1. 要件整理

### 1-1. プロダクト概要

各事業所（店舗）の Windows PC に接続した **シリアル接続の QR コードリーダー** でスタッフのQRを読み取り、勤務時刻を記録するデスクトップアプリケーション。

記録する打刻種別は4種。

| 打刻種別 | 内部値 | 用途 |
|---|---|---|
| 出勤 | `CLOCK_IN` | 始業 |
| 退勤 | `CLOCK_OUT` | 終業 |
| 中抜け開始 | `BREAK_START` | 一時退出 |
| 中抜け終了 | `BREAK_END` | 業務復帰 |

通常の休憩時間は打刻対象外（給与側で自動控除される前提）。**休憩自動控除ロジック自体も MVP 対象外。**

### 1-2. MVP 対象範囲

**含む**

- 待機画面（QR読取待ち）／打刻種別選択／打刻完了表示
- QRコードによるスタッフ特定と在籍状態チェック
- SQLite への打刻保存（オフライン完結）
- 管理者認証（店舗単位のPIN）
- 月次の打刻状況一覧・手動登録・修正
- CSV出力（UTF-8 BOM付き）
- スタッフ登録／編集／在籍状態変更
- QRコード発行・再発行（旧コード無効化）・カード画像出力
- 店舗設定（お知らせ編集、シリアルポート設定、接続テスト）
- エラー処理・アプリケーションログ

**含まない（将来拡張）**

会社サーバー通信 / 複数店舗の集中管理 / クラウド同期 / 給与計算 / シフト管理 / 通常休憩の打刻 / スマートフォンアプリ / Web管理画面。

ただし将来サーバー集約できるよう、**すべての主要データを店舗IDで識別可能な構造**とし、IDは衝突しない体系を採る（→ 5-2）。

### 1-3. 画面資料と仕様書の差分

Google Stitch のモックには、要件定義に無い要素が多数含まれている。**デザインの雰囲気は踏襲するが、以下は実装対象から外すか、確認のうえ決める。**

| モックの要素 | 仕様書の記載 | 本計画での扱い |
|---|---|---|
| 社員番号入力による打刻（テンキーモーダル） | 記載なし | **対象外**（→ 確認事項 Q5） |
| 顔写真・今月累計・所属/役職 | 記載なし | **対象外**（→ Q6） |
| 本日の店舗稼働サマリー（出勤済/予定/休憩中） | 記載なし | **対象外**。「予定」はシフト管理が前提で MVP 範囲外 |
| お知らせのカード表示／テロップ／件名＋最大5項目 | 「お知らせを編集できる」のみ | **要確認**（→ Q4） |
| ログイン画面の店舗選択（4店舗） | 事業所内で完結 | **要確認**（→ Q1） |
| 「即座にクラウドへ送信されます」 | クラウドは対象外 | **文言を変更**（誤解を招くため） |
| NTP同期・端末応答速度・通信正常 | 記載なし | **対象外**。時刻は端末時計を使用（→ Q20） |
| 通常打刻率／店舗稼働日数のKPIカード | 記載なし | **対象外**（集計コストに対し価値が薄い） |
| 「キオスク端末3台へQR暗号キー同期完了」 | 単一端末 | **対象外** |
| QRトークンの画面表示 `QT-E0104-9981A` | 推測困難な識別子 | **形式を変更**（→ Q14） |

---

## 2. 確認事項（実装前に決めたいこと）

実装に影響する順に並べる。

> **【高】Q1〜Q4 は 2026-09-05 に発注者判断で確定した。**（各項に「決定」として記載）
> Q2 の決定にともない**新たに Q2-a が派生している**。こちらは未確定のため、実装は推奨案を暫定採用する。

### 【高】Q1. 1端末が扱う店舗は1つか  ✅ 決定

モックのログイン画面には店舗選択ドロップダウン（渋谷店/新宿本店/池袋店/横浜店）があるが、MVP は「各事業所内で完結」とされている。

- **決定**: **1端末＝1店舗固定。SQLite は店舗専用**（1つのDBに複数店舗を混在させない）。
- 実装への反映:
  - ログイン画面に店舗選択ドロップダウンを**置かない**。設定済みの店舗名を表示するだけにする
  - `stores` は常に1行。起動時に読み込んで保持し、以降は画面ごとに取り直さない
  - それでも**全テーブルに `store_id` を持たせる**。将来サーバーへ集約したとき、店舗をまたいで束ねられる形を保つため

### 【高】Q2. 日をまたぐ勤務の帰属日をどう決めるか  ✅ 決定

22:00 出勤 → 翌 02:00 退勤の場合、**この勤務は何日の勤務として一覧・CSVに出るか。**

- **決定**: **店舗設定に「1日の開始時刻」「1日の終了時刻」を持ち、それで営業日を判定する。**
  （当初の推奨案「出勤日を引き継ぐ」方式は採らない）
- 実装への反映:
  - `stores` に `business_day_start` / `business_day_end`（`HH:mm`）を追加
  - `time_records.work_date` に**営業日**を保存する（列自体は当初案どおり必要）
  - 一覧・CSVの「日付」はカレンダー日付ではなく**営業日**を指す

**営業日の算出ルール**（開始 11:00 / 終了 翌 05:00 の店舗を例に）

```
基準日 base = 打刻時刻が開始時刻以降なら その日
              開始時刻より前なら       前日

  09/05 22:00 → 09/05 の営業日（22:00 >= 11:00）
  09/06 02:00 → 09/05 の営業日（02:00 <  11:00 なので前日）
  09/06 11:00 → 09/06 の営業日
```

- **終了時刻の用途**: 営業日の決定には使わない。**「営業時間外の打刻」を判定して警告を出すため**に使う
- 営業時間内の判定
  - `開始 < 終了`（例 09:00〜18:00、日中営業）: `開始 <= 時刻 < 終了`
  - `開始 > 終了`（例 11:00〜翌05:00、日跨ぎ営業）: `時刻 >= 開始` または `時刻 < 終了`
  - `開始 == 終了`: 24時間営業として常に営業時間内とする

**`work_date` は打刻の保存時に確定させ、以後は自動で再計算しない。** 後から手修正で退勤が追加されても勝手に変わらない。確定済みの月の集計が後日ずれると、給与計算のやり直しになるため。必要な場合は管理者が修正ダイアログで勤務日を直接変更する。

### 【高】Q2-a. 営業時間外（閉店中）の打刻をどちらの営業日に付けるか  ✅ 決定

Q2 の決定にともない派生した論点。**Q2 の算出ルールをそのまま当てると、開店前の打刻が前営業日に付いてしまう。**

- **決定**: 閉店中の打刻に限り、**直近の出勤が未退勤で、かつその出勤から18時間以内なら前営業日。それ以外は当日**とする。

開始 11:00 / 終了 翌 05:00 の店舗（閉店中は 05:00〜11:00）での結果:

| 起きること | 常に前営業日 | 常に当日 | **採用案** |
|---|---|---|---|
| 深夜勤務が延びて 06:00 に退勤 | ✅ | ❌ 退勤だけの行ができる | ✅ 前営業日 |
| 中抜けから 09:00 に復帰 | ✅ | ❌ 中抜け終了だけ翌日行に | ✅ 前営業日 |
| 開店準備で 10:00 に出勤 | ❌ 前日の勤務に混ざる | ✅ | ✅ 当日 |
| 早朝清掃で 06:00 に出勤 | ❌ | ✅ | ✅ 当日 |

**18時間の上限を置く理由**: 上限が無いと、**前日に退勤を忘れた人が翌朝出勤したときに「未退勤あり」と誤判定**され、前営業日に付いてしまう。退勤忘れは現場で頻繁に起きる。深夜勤務の延長は長くても十数時間で収まる一方、退勤忘れは前日の出勤から20時間以上経っていることがほとんどなので、この1本で切り分けられる。

**採らなかった案**: 設定で3択（前営業日／当日／未退勤で判定）にする方式。柔軟だが、店舗の担当者が意味を理解して選ぶ必要があり、設定を間違えると勤怠が静かにずれる。小規模事業所向けという前提では、正しく動く1つの挙動を埋め込むほうが事故が少ない。

**それでも取りこぼしは残る。** 管理者が勤務状況画面から**営業日を含めて修正できる**ようにすることで担保する（→ 11-4-4）。判定は `BusinessDayResolver` 1クラスに閉じる（→ 6-7）。

### 【高】Q3. 1日に中抜けが複数回ある場合の表現  ✅ 決定

仕様の一覧・CSVの列は「中抜け開始」「中抜け終了」が各1つ。実際には複数回発生しうる。

- **決定**: 推奨案どおり。1日1行を維持し、`中抜け開始`＝最初の開始、`中抜け終了`＝最後の終了、加えて `中抜け回数` と `中抜け合計(分)` を列として持つ。
- 理由: CSVは給与担当が Excel で扱う。行数が可変だと集計しづらい。

### 【高】Q4. 「店舗からのお知らせ」の構造  ✅ 決定

3つの資料で形式が食い違っている。

| 資料 | 形式 |
|---|---|
| 仕様書 | 「文章」1つ |
| メイン画面.png | カード3枚（カテゴリチップ・期限・本文） |
| 管理者_店舗設定.png | 件名（最大40文字）＋ 項目リスト最大5件（見出し＋本文）＋ テロップ表示切替 |

- **決定**: 推奨案どおり。「件名（40文字）＋ 項目最大5件（見出し30文字＋本文200文字）」とし、メイン画面ではカード形式で表示する。テロップ（スクロール）は将来。
- 理由: 単一テキストだとメイン画面のレイアウトが作れない。テロップは実装コストの割に可読性が落ちる。

### 【高】Q5. 社員番号入力による打刻を MVP に含めるか

メイン画面モックに「QRコードをお持ちでない方はこちら（社員番号入力）」ボタンとテンキーモーダルがある。仕様書には記載がない。

- **推奨案**: **MVP対象外**。
- 理由: 社員番号は他人が知りうる値で、QR運用の本人確認を無効化する。必要なら「QRを忘れた場合は管理者が手動登録する」で代替できる（勤務状況タブの打刻手動追加）。

### 【中】Q6. スタッフに持たせる属性の範囲

仕様書のスタッフ項目は「スタッフID / 店舗ID / 氏名 / QR識別子 / 在籍状態 / 作成日時 / 更新日時」。一方 `prompt.txt` には「社員／アルバイトなどの**区分**を登録できる」とあり、モックには社員番号・フリガナ・区分・所属/役職・顔写真がある。

- **推奨案**: `社員番号`・`フリガナ`・`区分`（社員／アルバイト／パート）まで持つ。役職・顔写真は将来。
- 理由: 社員番号は CSV を給与システムへ渡すときの突合キーになる。フリガナは名簿の並び替えと検索に要る。区分は要件に明記がある。

### 【中】Q7. 異常な打刻を禁止するか警告にとどめるか

「出勤 → 出勤」「退勤 → 退勤」「出勤前の中抜け開始」など。

- **推奨案**: **警告して記録は残す**（ブロックしない）。異常フラグを立て、勤務状況画面で色分けして管理者が後から修正する。
  - 例外として、**同一種別の60秒以内の重複だけは保存しない**（ボタン連打・二度読みの抑止）。
- 理由: 現場で「打刻できない」ことの損害のほうが大きい。誤りは管理者が直せる設計（手修正機能がある）なので、記録を優先する。

### 【中】Q8. 退勤後、同じ日に再び出勤できるか

- **推奨案**: 可能とする（Q7 と同じ方針で警告のみ）。一覧では2行目として表示。
- 理由: 早番→中抜け→遅番のような運用が飲食・小売では起こる。

### 【中】Q9. 管理者パスワードの初期設定方法

仕様書に初期値・初期設定手順の記載がない。

- **推奨案**: 初回起動時にセットアップ画面を出し、**店舗名・会社名・管理者PIN** を登録させる。既定パスワードは持たない。
- 理由: 初期パスワードを固定値にすると、変更されないまま運用される。

### 【中】Q10. 認証5回失敗時の挙動

仕様書は「エラーメッセージ表示 → メイン画面へ戻る」。モックは「10分間ロック」。

- **推奨案**: 両方実施。メイン画面へ戻したうえで、**その端末を5分間ロック**する。ロック解除時刻は DB に保存する。
- 理由: 画面を戻すだけでは即座に再試行でき、8桁数字（1億通り）に対する総当たりを止められない。ロック状態をメモリに置くと再起動で回避されるため DB に置く。

### 【中】Q10-a. 管理画面を開いたまま放置されたときの扱い（10章の実装で判明）

管理画面にいる間は打刻を受け付けないため、**管理者が操作の途中で離れると、誰も打刻できない端末になる**。
仕様書・モックのどちらにも記載がない。

- **ログイン画面**: 無操作60秒で待機画面へ戻す実装にした。
  この画面には保存すべき入力が無く、戻して失うものが無いため。
- **管理画面（ログイン後）**: **自動ログアウトを入れていない。** 判断が要るため。
  - 入れる場合の懸念: 打刻の手修正やスタッフ登録の入力中に画面が消え、入力が失われる
  - 入れない場合の懸念: 閉じ忘れると翌朝まで打刻できない
- **推奨案**: 管理画面にも自動ログアウトを入れる（無操作10分）。ただし**入力途中の画面では働かせない**
  （編集ダイアログを開いている間はタイマーを止める）。
- 理由: 打刻できない時間が長く続くほうが損害が大きい。一方で入力の喪失は現場の不信につながるため、
  編集中だけは例外にする。

### 【中】Q11. QRカードの出力形式をどこまで実装するか

`prompt.txt` は「カード形式の画像として発行し、LINEやメールで配布」。モックには「カード印刷（名刺）」「画像保存(PNG)」がある。

- **推奨案**: MVP は **PNG保存のみ**。印刷・PDFは将来。
- 理由: 配布要件（LINE/メール）は画像で満たせる。WPF の印刷は用紙設定・プレビューまで作ると工数が大きい。

### 【低】Q12. シリアルの終端文字を設定可能にするか

仕様のシリアル設定項目は COMポート／BaudRate／DataBits／Parity／StopBits のみで、終端文字がない。

- **推奨案**: 設定を持たず、**CR / LF / CRLF のいずれでも1件の区切りとして扱う**（空行は無視）。加えて、無受信 200ms でも1件確定とするフォールバックを入れる。
- 理由: 機種差を設定なしで吸収できる。

### 【低】Q13. DataBits / Parity / StopBits を UI で編集可能にするか

モックでは固定表示に見える（8bit / None / 1bit）。

- **推奨案**: 編集可能にする（仕様に「設定可能とする」と明記があるため）。ただし既定値 8/None/1 を初期表示し、通常は触らせない配置にする。

### 【低】Q14. QRトークンの形式とカード上の表示

モックの `QT-E0104-9981A` は社員番号が読み取れ、「推測困難」という要件を満たさない。

- **推奨案**: **160bit の暗号学的乱数を Base32（32文字）** で表現する。カード上には表示しない（QR画像のみ）。
- 理由: UUIDv4 でも良いが、Base32 は QR のバージョンが小さく済み、読み取りが速い。人が読む必要がないため可読性は不要。

### 【低】Q15. 退職時にQRを自動で無効化するか

- **推奨案**: 在籍状態を「退職済」に変更した時点で、有効なQRトークンを自動的に失効させる（確認ダイアログあり）。
- 理由: 手動失効を忘れると、退職者のQRで打刻できてしまう。

### 【低】Q16. 会社名・会社ロゴの登録方法

店舗設定では「編集不可」とされているが、誰がどこで登録するかの記載がない。

- **推奨案**: 初回セットアップ画面で登録し、以降は店舗設定で読み取り専用表示。ロゴ画像は MVP では未対応（アプリ名テキストのみ）。

### 【低】Q17. 端末時計のずれをどう扱うか

モックには「NTP同期済」表示があるが、MVP はオフライン前提のため同期手段がない。**端末時計がずれると勤怠がそのままずれる。**

- **推奨案**: アプリでは同期しない。運用手順として Windows の時刻同期を有効にしておくことを導入手順書に明記し、管理画面に現在時刻を常時表示して気づけるようにする。

### 【低】Q18. スタッフ本人による打刻の取り消し

- **推奨案**: 不可（管理者の修正機能で対応）。
- 理由: 本人が消せると打刻の証跡にならない。

### 【低】Q19. 全画面（キオスク）モードの要否

モックにはウィンドウの最小化・最大化・閉じるボタンが描かれている。

- **推奨案**: 通常ウィンドウの最大化起動。キオスク固定（閉じるを無効化）は将来。

### 【低】Q20. 修正理由メモの保持

モックの勤務状況一覧に「修正理由 / メモ」列があるが、仕様書は「修正履歴は保持しない」。

- **推奨案**: **履歴は持たないが、最新の理由メモ1件は打刻レコードに保持する**（`note` 列）。
- 理由: 監査ではなく、管理者自身が後から見て思い出すためのもの。列1つで済む。

---

## 3. システムアーキテクチャ

### 3-1. 技術選定

| 項目 | 採用 | 理由 |
|---|---|---|
| ランタイム | **.NET 10（LTS）** | 検証環境にSDK 10.0.400 導入済み。VS2026 の標準ターゲット。LTS のためサポート期間が長い |
| UI | **WPF** | 要件で指定。タッチ対応・オフライン・シリアル通信に適する |
| MVVM 基盤 | **CommunityToolkit.Mvvm** | `[ObservableProperty]` / `[RelayCommand]` のソースジェネレータで定型コードを削減。MITライセンス |
| DI | **Microsoft.Extensions.DependencyInjection / Hosting** | 標準。テスト時の差し替えが容易 |
| DB | **SQLite + EF Core 10** | マイグレーションでスキーマ変更を管理でき、テストで一時DBへ差し替えやすい |
| シリアル | **System.IO.Ports** | .NET 標準（NuGet パッケージ） |
| QR生成 | **QRCoder** | 純.NET・ネイティブ依存なし・MIT |
| ログ | **Serilog + Sinks.File** | 日次ローテーションと保持世代数が設定1行で書ける |
| CSV | **自前実装** | 列固定で数も少ない。CsvHelper は MVP には過剰 |
| テスト | **xUnit** | 標準的。CI に載せやすい |

> **EF Core を選ぶ理由（Dapper との比較）**: テーブル数は6程度で、Dapper でも十分書ける。それでも EF Core を採るのは、**スキーマ変更をマイグレーションとして残せる**ため。MVP後にスタッフ属性やお知らせ構造が変わる可能性が高く（→ 確認事項 Q4/Q6）、その差分をコードとして追跡できる価値が、ORM の学習・起動コストを上回る。

### 3-2. アーキテクチャ方針

MVVM を基本とし、**View / ViewModel / Service / Repository / Device** に責務を分ける。

```
 View (XAML)          画面。コードビハインドは原則フォーカス制御のみ
   │ Binding / Command
 ViewModel            画面状態。UIスレッド。ロジックは持たず Service を呼ぶ
   │
 Service              ユースケース（打刻する / CSVを出す / 認証する）
   │                  ここに業務ルールを集約。WPF に依存しない
 Repository           永続化。EF Core の DbContext を隠す
   │
 SQLite               ローカルDB
```

シリアル通信は独立した系統として扱う。

```
 SerialPort ──▶ ScanBuffer ──▶ QrScannerService ──(event)──▶ ViewModel
                （終端で分割）   （接続管理・再接続）
```

**過剰設計を避けるための線引き**

- インターフェースは**テストで差し替えるものだけ**作る（`ISerialPort`、`IClock`、各 Repository）。ViewModel ごとのインターフェースは作らない
- CQRS・イベントソーシング・メッセージバスは導入しない
- Repository は「テーブル1つにつき1クラス」に留め、汎用リポジトリ基底クラスは作らない

### 3-3. プロジェクト構成

```
QTimeRecord.slnx
│
├─ QTimeRecord.Core/            クラスライブラリ（WPF非依存・テスト対象の中心）
│   ├─ Domain/                  エンティティ・列挙型・状態遷移・集計
│   ├─ Data/                    DbContext・マイグレーション・Repository
│   ├─ Services/                ユースケース
│   ├─ Devices/                 シリアル通信・QR受信
│   └─ Infrastructure/          パス解決・ログ設定・アプリ設定
│
├─ QTimeRecord.App/             WPF 本体
│   ├─ Views/                   画面
│   ├─ ViewModels/
│   ├─ Controls/                テンキー・タッチボタン等の共通部品
│   ├─ Resources/               DESIGN.md 由来のスタイル辞書
│   └─ Services/                Navigation / Dialog / IdleTimeout
│
└─ QTimeRecord.Tests/           xUnit
```

> **3プロジェクトに留める理由**: Core を Data / Devices / Services に物理分割する構成も一般的だが、MVP の規模では参照関係の管理コストだけが増える。**「WPF から切り離してテストできる」ことが目的**なので、Core 内はフォルダと名前空間で分ける。将来 Core が肥大したら、フォルダ単位でプロジェクトに切り出せる形にしておく。

---

## 4. プロジェクト初期構築

- [x] **4-1. ソリューションとプロジェクトの作成**
  - `QTimeRecord.slnx` / `QTimeRecord.Core`(classlib) / `QTimeRecord.App`(wpf) / `QTimeRecord.Tests`(xunit)
  - ターゲット: `net10.0-windows`（App）/ `net10.0`（Core・Tests）
  - `Directory.Build.props` に共通設定（`Nullable=enable`, `TreatWarningsAsErrors=true`, `LangVersion=latest`, `InvariantGlobalization=false`）
  - **完了条件**: `dotnet build` が警告0で通る／`dotnet run --project QTimeRecord.App` で空ウィンドウが出る

- [x] **4-2. NuGet パッケージの導入**
  - Core: `Microsoft.EntityFrameworkCore.Sqlite`, `Microsoft.EntityFrameworkCore.Design`, `System.IO.Ports`, `QRCoder`, `Serilog`, `Serilog.Sinks.File`, `Serilog.Extensions.Logging`
  - App: `CommunityToolkit.Mvvm`, `Microsoft.Extensions.Hosting`
  - Tests: `xunit`, `xunit.runner.visualstudio`, `Microsoft.NET.Test.Sdk`
  - **完了条件**: バージョンを `Directory.Packages.props`（中央管理）に固定し、復元が通る

- [x] **4-3. DI とアプリ起動処理**
  - `App.xaml.cs` で `IHost` を構築。登録は `AppComposition.AddAppServices` に切り出し、
    テストから同じ構成のコンテナを組み立てられるようにする
  - **完了条件**: ViewModel が DI 経由で解決され、MainWindow が表示される
  - **テスト観点**: DI コンテナの解決漏れ（`ValidateOnBuild` ＋ 全 ViewModel を解決するスモークテスト）
  - ⚠ **DbContext・Service・Repository の登録と、起動時の DB マイグレーション／初回起動判定は
    この工程では実施していない**（対象クラスが 5 章で作られるため）。5-5-2 と 5-5-4 で
    `AddAppServices` に追記する
  - テストプロジェクトは App の DI 構成を検証するため `net10.0-windows` を対象にした

- [x] **4-4. アプリ設定ファイル**
  - `appsettings.json` に **起動前に必要な最小設定のみ**（DBファイルパス・ログ出力先・ログレベル）
  - **完了条件**: 設定を変更するとDB・ログの出力先が変わる

- [x] **4-5. デザイントークンのリソース化**
  - `Resources/Colors.xaml`: DESIGN.md の全色を `SolidColorBrush` として定義（キー名は `Brush.Primary` 等）
  - `Resources/Typography.xaml`: `clock-display` / `headline-lg` / `kiosk-button` / `tabular-data` などを `Style TargetType=TextBlock` で定義
  - `Resources/Buttons.xaml`: プライマリ／セカンダリ／キーパッドのボタンスタイル（最小高 64px、キーパッド 56px、押下時のフィードバック）
  - フォント: 現時点では**同梱せず**、`Noto Sans JP → Yu Gothic UI → Meiryo UI` のフォールバックで代替する。
    同梱する場合は SIL OFL 1.1 の全文表示が必要になるため、判断を分けた（→ 4-6）
  - `Colors.xaml` / `Typography.xaml` は **DESIGN.md の front matter から生成した**。
    色や文字サイズを変えるときは DESIGN.md 側を直すこと（手で転記すると必ずどれか間違える）
  - **完了条件**: 色・文字サイズをハードコードした XAML が1つも無い状態を保てる
  - **テスト観点**: 1366x768 と 1920x1080 でレイアウトが破綻しないこと（手動）

- [x] **4-6. サードパーティ ライセンス表示**
  - `THIRD-PARTY-NOTICES.txt` を配布物に同梱する
  - 対象は**配布物に実際に含まれるパッケージ**（実行時アセットを持つもの）。ビルド専用の
    ツール・解析器は含めない。推移的依存を取りこぼさないよう `project.assets.json` から起こす
  - 現状: MIT 55 件 / Apache-2.0 7 件。NOTICE ファイルを同梱するパッケージは無いため、
    Apache-2.0 条項 4.4 による追加表示は不要
  - **依存を追加・更新したら必ず更新する**
  - **完了条件**: 全依存のライセンス種別と著作権表示が記載され、MIT・Apache-2.0 の全文が含まれる
  - **テスト観点**: `dotnet list package --include-transitive` の結果と記載内容が一致すること

> **フォントを同梱するかは別途判断が必要。** 同梱すると表示が指定どおりになる一方、
> SIL OFL 1.1 の全文表示が増え、配布物のサイズも増える。OS 標準フォントで代替する場合、
> 字形が変わるため**モックとの見え方の差**を許容することになる。

---

## 5. DB設計・実装

### 5-1. テーブル一覧

| テーブル | 役割 |
|---|---|
| `stores` | 店舗・会社情報・お知らせ |
| `announcements` | お知らせ項目（Q4 の推奨案を採る場合） |
| `staff` | スタッフ |
| `staff_qr_tokens` | QRトークン（発行履歴・失効管理） |
| `time_records` | 打刻（本体） |
| `device_settings` | シリアルポート設定 |
| `admin_credentials` | 管理者PIN（ハッシュ）とロック状態 |

### 5-2. ID体系

**すべての主キーは UUIDv7（`Guid.CreateVersion7()`）の文字列表現**とする。

- 将来サーバーへ集約したとき、店舗をまたいでも衝突しない
- v4 ではなく v7 を選ぶのは、**生成時刻順にソートされ、主キーインデックスの断片化が起きにくい**ため
- SQLite には `TEXT`（ハイフン付き36文字・大文字）で保存する。BLOB より SQL で確認しやすい

人が読むための `店舗コード` `社員番号` は**別の列**として持つ（主キーにしない）。→ Q2/Q6

### 5-3. 日時の持ち方

- 型: `TEXT`。C# 側は **`DateTime`（ローカルの壁時計時刻）** で保持する
- 書式は EF Core の既定（`2026-09-05 13:53:17`）。文字列として辞書順に並べれば時刻順になる
- `work_date`（営業日）だけは `TEXT` の `YYYY-MM-DD`。カレンダー日付ではなく、店舗設定の開始時刻で切った営業日を入れる（→ Q2）

> **⚠ 当初は `DateTimeOffset`（オフセット付き ISO8601）を採る計画だったが、実装時に撤回した。**
>
> **SQLite は `DateTimeOffset` の `ORDER BY` と大小比較をサポートしない**（`SQLite does not support
> expressions of type 'DateTimeOffset' in ORDER BY clauses`）。打刻は「時刻順に並べる」「直近の1件を取る」
> 「この時刻より後の退勤があるか」を常に問うため、この制約は致命的になる。回避するにはクライアント側で
> 並べ替えることになり、月次一覧が索引を使えず全件読み込みになる。
>
> **壁時計時刻を選ぶ理由**（UTC 保存ではなく）
>
> - 勤怠として意味を持つのは「打刻機に表示された時刻」そのもの。タイムカードの記録と DB の値が一致する
> - UTC 保存は変換を1か所でも誤ると全件が9時間ずれる。ずれても一見それらしく見えるため発見が遅れる
> - 端末のタイムゾーン設定を変えても、既存の記録の表示が変わらない
> - 日本国内の単一事業所が前提で、夏時間が無く、オフセットは常に +09:00。offset を持つ実益が小さい
>
> **将来サーバーへ集約するときの備え**: 店舗が国内に閉じている限り問題にならない。国をまたぐ運用が
> 現実になった時点で、`stores` にタイムゾーン列を追加し、集約側で解釈する。
> 打刻レコードの構造は変えずに済む。

### 5-4. スキーマ定義

以下は **`Data/Migrations/*_InitialCreate.cs` から実際に生成される SQL** （`dotnet ef migrations script` の出力）。
スキーマの正はマイグレーションであり、この節はその写し。**手でここだけ直さないこと。**

```sql
CREATE TABLE "stores" (
    "id" TEXT NOT NULL CONSTRAINT "pk_stores" PRIMARY KEY,
    "store_code" TEXT NULL,
    "store_name" TEXT NOT NULL,
    "company_name" TEXT NOT NULL,
    "announcement_title" TEXT NULL,
    "business_day_start" TEXT NOT NULL,
    "business_day_end" TEXT NOT NULL,
    "created_at" TEXT NOT NULL,
    "updated_at" TEXT NOT NULL
);

CREATE TABLE "admin_credentials" (
    "store_id" TEXT NOT NULL CONSTRAINT "pk_admin_credentials" PRIMARY KEY,
    "password_hash" TEXT NOT NULL,
    "salt" TEXT NOT NULL,
    "iterations" INTEGER NOT NULL,
    "failed_count" INTEGER NOT NULL,
    "locked_until" TEXT NULL,
    "updated_at" TEXT NOT NULL,
    CONSTRAINT "fk_admin_credentials_stores_store_id" FOREIGN KEY ("store_id") REFERENCES "stores" ("id") ON DELETE CASCADE
);

CREATE TABLE "announcements" (
    "id" TEXT NOT NULL CONSTRAINT "pk_announcements" PRIMARY KEY,
    "store_id" TEXT NOT NULL,
    "display_order" INTEGER NOT NULL,
    "heading" TEXT NOT NULL,
    "body" TEXT NOT NULL,
    "created_at" TEXT NOT NULL,
    "updated_at" TEXT NOT NULL,
    CONSTRAINT "fk_announcements_stores_store_id" FOREIGN KEY ("store_id") REFERENCES "stores" ("id") ON DELETE CASCADE
);

CREATE TABLE "device_settings" (
    "store_id" TEXT NOT NULL CONSTRAINT "pk_device_settings" PRIMARY KEY,
    "com_port" TEXT NULL,
    "baud_rate" INTEGER NOT NULL,
    "data_bits" INTEGER NOT NULL,
    "parity" TEXT NOT NULL,
    "stop_bits" TEXT NOT NULL,
    "updated_at" TEXT NOT NULL,
    CONSTRAINT "fk_device_settings_stores_store_id" FOREIGN KEY ("store_id") REFERENCES "stores" ("id") ON DELETE CASCADE
);

CREATE TABLE "staff" (
    "id" TEXT NOT NULL CONSTRAINT "pk_staff" PRIMARY KEY,
    "store_id" TEXT NOT NULL,
    "staff_no" TEXT NULL,
    "name" TEXT NOT NULL,
    "name_kana" TEXT NULL,
    "employment_type" TEXT NULL,
    "status" TEXT NOT NULL,
    "created_at" TEXT NOT NULL,
    "updated_at" TEXT NOT NULL,
    CONSTRAINT "fk_staff_stores_store_id" FOREIGN KEY ("store_id") REFERENCES "stores" ("id") ON DELETE RESTRICT
);

CREATE TABLE "staff_qr_tokens" (
    "id" TEXT NOT NULL CONSTRAINT "pk_staff_qr_tokens" PRIMARY KEY,
    "store_id" TEXT NOT NULL,
    "staff_id" TEXT NOT NULL,
    "token" TEXT NOT NULL,
    "issued_at" TEXT NOT NULL,
    "revoked_at" TEXT NULL,
    "created_at" TEXT NOT NULL,
    "updated_at" TEXT NOT NULL,
    CONSTRAINT "fk_staff_qr_tokens_staff_staff_id" FOREIGN KEY ("staff_id") REFERENCES "staff" ("id") ON DELETE RESTRICT
);

CREATE TABLE "time_records" (
    "id" TEXT NOT NULL CONSTRAINT "pk_time_records" PRIMARY KEY,
    "store_id" TEXT NOT NULL,
    "staff_id" TEXT NOT NULL,
    "record_type" TEXT NOT NULL,
    "recorded_at" TEXT NOT NULL,
    "work_date" TEXT NOT NULL,
    "entry_method" TEXT NOT NULL,
    "note" TEXT NULL,
    "is_outside_business_hours" INTEGER NOT NULL,
    "created_at" TEXT NOT NULL,
    "updated_at" TEXT NOT NULL,
    CONSTRAINT "fk_time_records_staff_staff_id" FOREIGN KEY ("staff_id") REFERENCES "staff" ("id") ON DELETE RESTRICT
);

CREATE INDEX "ix_announcements_store_id_display_order" ON "announcements" ("store_id", "display_order");

CREATE UNIQUE INDEX "ix_staff_store_id_staff_no" ON "staff" ("store_id", "staff_no") WHERE staff_no IS NOT NULL;

CREATE INDEX "ix_staff_store_id_status" ON "staff" ("store_id", "status");

CREATE INDEX "ix_staff_qr_tokens_staff_id_revoked_at" ON "staff_qr_tokens" ("staff_id", "revoked_at");

CREATE UNIQUE INDEX "ix_staff_qr_tokens_token" ON "staff_qr_tokens" ("token");

CREATE INDEX "ix_time_records_staff_id_recorded_at" ON "time_records" ("staff_id", "recorded_at");

CREATE INDEX "ix_time_records_staff_id_work_date" ON "time_records" ("staff_id", "work_date");

CREATE INDEX "ix_time_records_store_id_work_date" ON "time_records" ("store_id", "work_date");
```

**設計上の要点**

- **主キーはすべて `id`**。FK 側は `store_id` / `staff_id` と対になる
- **列挙型は大文字スネークケースの文字列**（`CLOCK_IN` / `ON_LEAVE` / `MANUAL_ADD`）。
  数値だと DB を直接開いたときに意味が読めない。変換は C# の名前から機械的に導く
- **`time_records.work_date` は営業日**。`recorded_at` とは日付がずれることがある（→ Q2）
- **`time_records.is_outside_business_hours`** は営業時間外の打刻を示す。
  勤務状況の一覧で「要確認」として色分けするために持つ（→ Q2-a）
- **外部キーの削除時動作**

  | 参照 | 動作 | 理由 |
  |---|---|---|
  | `staff` → `stores` | RESTRICT | スタッフが残る店舗を消させない |
  | `staff_qr_tokens` → `staff` | RESTRICT | 発行履歴を巻き添えで消さない |
  | `time_records` → `staff` | **RESTRICT** | **スタッフの物理削除で打刻が道連れになるのを DB 側で止める** |
  | `announcements` / `device_settings` / `admin_credentials` → `stores` | CASCADE | 店舗に従属する設定のため |

- **打刻の重複は DB 制約で防がない。** 「同一スタッフ・同一種別・同時刻」を一意にすると、
  正当な再出勤（→ Q8）まで弾く。連打対策はアプリ側の60秒ルールで行う（→ Q7）
- 接続ごとに `PRAGMA foreign_keys=ON` / `journal_mode=WAL` / `synchronous=FULL` を適用する
  （`SqlitePragmaInterceptor`）。`foreign_keys` は既定 OFF のため、指定しないと上の RESTRICT が効かない

### 5-5. 実装工程

- [x] **5-5-1. エンティティと列挙型の定義**
  - `Domain/Entities/`: `Store`, `Announcement`, `Staff`, `StaffQrToken`, `TimeRecord`, `DeviceSettings`, `AdminCredential`
  - `Domain/Enums/`: `StaffStatus`, `TimeRecordType`, `EntryMethod`
  - 列挙型は DB へ**文字列として保存**する（数値だと DB を直接見たときに読めない）
  - **完了条件**: 列挙型と DB 値の相互変換にテストがある

- [x] **5-5-2. DbContext とマイグレーション**
  - `Data/QTimeRecordDbContext.cs`、`OnModelCreating` でインデックス・変換を定義
  - 初回マイグレーション `InitialCreate` を作成
  - 接続時に `PRAGMA journal_mode=WAL` / `PRAGMA foreign_keys=ON` / `PRAGMA synchronous=FULL` を適用
  - **完了条件**: 空のDBファイルからマイグレーションでスキーマが構築される
  - **テスト観点**: マイグレーション適用後に全テーブル・インデックスが存在すること

> **`synchronous=FULL` を選ぶ理由**: 打刻は1日数十件程度で書き込み頻度が低く、性能上の不利がほぼない。一方で「電源断で直前の打刻が消える」ことは勤怠として許容できない。書き込みごとに確実にディスクへ同期させる。

- [x] **5-5-3. Repository の実装**
  - `Data/Repositories/`: `StoreRepository`, `StaffRepository`, `QrTokenRepository`, `TimeRecordRepository`, `DeviceSettingsRepository`, `AdminCredentialRepository`
  - 各インターフェースを定義（テストで差し替えるため）
  - **完了条件**: CRUD が一時DBファイルに対するテストで通る
  - **テスト観点**: トークン検索が失効済みを除外すること／月次取得が範囲境界（月初・月末）を含むこと

- [x] **5-5-4. 初回セットアップ処理**
  - DB が空の場合、店舗・管理者PIN・会社名を登録する初期化サービス（→ Q9）
  - **完了条件**: 空DBから起動するとセットアップ画面が出て、完了後に通常起動する

---

## 6. 共通処理

- [x] **6-1. アプリケーションパスの解決**
  - `Infrastructure/AppPaths.cs`
  - DB: `%LOCALAPPDATA%\QTimeRecord\qtimerecord.db`
  - ログ: `%LOCALAPPDATA%\QTimeRecord\logs\`
  - CSV既定の保存先: ユーザーの「ドキュメント」
  - **完了条件**: フォルダが無ければ起動時に作成される

> **`%LOCALAPPDATA%` に置く理由**: `Program Files` 配下は書き込みに管理者権限が要り、標準ユーザーで運用する店舗PCでは打刻が保存できなくなる。複数ユーザーで同一データを共有する要件が出た場合は `%ProgramData%` に移すが、MVP は1端末1アカウント運用のため不要。

- [x] **6-2. 時刻の抽象化**
  - `IClock` / `SystemClock`（`DateTimeOffset.Now` を返す）
  - **完了条件**: ロジックが `DateTime.Now` を直接呼ばない
  - **テスト観点**: 固定時刻で状態遷移・集計・日跨ぎをテストできること

- [x] **6-3. ログ基盤** — 実体は 15-4-1 で実装した（同じ作業を二重に書かない）
- [x] **6-4. 例外ハンドリング基盤** — 実体は 15-4-2 で実装した
- [x] **6-5. ナビゲーションサービス**
  - `Services/NavigationService.cs`: `MainWindow` の `ContentControl` を切り替える
  - 画面はウィンドウを増やさず単一ウィンドウ内で遷移（キオスク用途のため）
  - **完了条件**: ViewModel から画面遷移を指示でき、View 側にコードビハインドが増えない

- [x] **6-6. ダイアログサービス**
  - 確認・エラー・入力ダイアログを ViewModel から呼べるようにする
  - タッチ操作に耐えるボタンサイズで統一
  - **完了条件**: `MessageBox` を直接呼ぶ箇所が無い

- [x] **6-7. 営業日の算出**（→ Q2 / Q2-a）
  - `Domain/BusinessDayResolver.cs`
    - `Resolve(打刻時刻, 店舗の営業日設定, 直近の未退勤の出勤時刻) → 営業日`
    - `IsWithinBusinessHours(打刻時刻, 店舗の営業日設定) → bool`
  - 判定ルール（Q2 / Q2-a の決定）

    ```
    営業時間内           → 打刻時刻 >= 開始時刻 なら その日、そうでなければ 前日
    営業時間外（閉店中） → 直近の出勤が未退勤 かつ その出勤から18時間以内 → 前営業日
                           それ以外                                     → 当日
    ```

  - 18時間の上限は「退勤忘れ」を「勤務継続中」と誤判定しないためのもの。定数は
    このクラスに置き、根拠をコメントで残す
  - **打刻・日次集計・CSV のすべてがここを通す。** 営業日の導出をこのクラス以外に書かない
  - **完了条件**: 日中営業／日跨ぎ営業／24時間営業の3形態で正しい営業日を返す
  - **テスト観点**: 開始時刻ちょうど／開始1分前／終了時刻ちょうど／閉店中に未退勤あり・なし／
    未退勤だが18時間超（退勤忘れ）／日跨ぎ営業で 00:00 をまたぐ／月をまたぐ（月末→翌月1日）／うるう日

> **`work_date` を毎回計算せず列に保存する理由**: 店舗設定の開始時刻を後から変更しても、**確定済みの過去データが遡って書き換わらない**ようにするため。都度計算にすると、設定変更した瞬間に先月の勤怠が変わる。

---

## 7. QRコード・シリアル通信

### 7-1. 受信の流れ

```
SerialPort.DataReceived
      │ バイト列
      ▼
ScanBuffer      改行(CR/LF/CRLF)で分割。空行は捨てる
      │         200ms 無受信でも1件として確定（終端を送らない機種への保険）
      ▼
QrScannerService  トリム → 妥当性チェック → ScannedEvent 発火
      │
      ▼ Dispatcher 経由で UI スレッドへ
MainViewModel
```

### 7-2. 接続管理

| 状態 | 挙動 |
|---|---|
| 未接続 | メイン画面に「リーダー未接続」を赤系で表示。打刻不可 |
| 接続中 | 「リーダー待機中」を緑系で表示 |
| 接続断を検知 | 状態を未接続に変更し、**自動再接続を開始**。打刻画面が開いていればキャンセル以外を無効化 |
| 再接続試行 | 1s → 2s → 5s → 10s → 30s（以降30s固定）のバックオフ |

**接続断の検知方法**（複数併用）

1. `SerialPort.ErrorReceived` イベント
2. 読み取り時の `IOException` / `OperationCanceledException`
3. 5秒周期のポーリングで `SerialPort.GetPortNames()` に自ポートが存在するか確認（USB 抜去はこれで確実に拾える）

**排他制御**: シリアルポートは排他利用のため、店舗設定画面で「接続テスト」を実行する間は、メインの受信を停止してからテストし、終了後に復帰させる。これを守らないと `UnauthorizedAccessException` が出る。

### 7-3. 実装工程

- [x] **7-3-1. シリアルポートの抽象化**
  - `Devices/ISerialPort.cs`（Open/Close/DataReceived/ErrorReceived/IsOpen）
  - `Devices/SystemSerialPort.cs`（`System.IO.Ports.SerialPort` のラッパー）
  - `Devices/FakeSerialPort.cs`（テスト用。任意のバイト列を流せる）
  - **完了条件**: 実機なしで受信処理をテストできる

- [x] **7-3-2. 受信バッファ**
  - `Devices/ScanBuffer.cs`: 分割受信の結合、終端での確定、タイムアウト確定、最大長制限（異常データ保護）
  - **完了条件**: 1文字ずつ分割して流しても1件として復元できる
  - **テスト観点**: CR / LF / CRLF の3種／2件連続受信／終端なしタイムアウト／制御文字の混入／長すぎる入力の破棄

- [x] **7-3-3. QRスキャナサービス**
  - `Devices/QrScannerService.cs`: 接続・切断・疎通確認・再接続・イベント通知
  - `Devices/SerialPortEnumerator.cs`: 利用可能COMポート一覧の取得
  - **完了条件**: 接続状態が `ConnectionState` として購読でき、UI に反映される
  - **テスト観点**: 接続断→再接続のバックオフ間隔／再接続後に受信が再開すること

- [x] **7-3-4. QRトークンの生成と解決**
  - `Services/QrTokenService.cs`
    - 生成: `RandomNumberGenerator` で16バイト → Base32（32文字）
    - 解決: トークン → 有効なレコード → スタッフ（失効済み・未登録は明確に区別して返す）
  - **完了条件**: 発行・失効・再発行が一連で動く
  - **テスト観点**: 失効済みトークンで解決できないこと／再発行で旧トークンが失効すること／未登録トークンの判別

- [x] **7-3-5. QR画像・カード画像の生成**
  - Core は QR 画像だけを作り（`QrCodeImageGenerator`）、カードへの合成は App 側で WPF 描画で行う（`QrCardRenderer`）。
    System.Drawing を持ち込まずに済み、画面と同じフォント・配色で描ける
  - カードに載せる情報: 店舗名・スタッフ氏名・社員番号・発行日（**トークン文字列は載せない** → Q14）
  - **完了条件**: PNG として保存でき、実機のリーダーで読み取れる
  - **テスト観点**: 生成した画像サイズ・DPI／氏名が長い場合の省略／実機読み取り（実機テスト）

---

## 8. メイン画面

参照: [img/メイン画面.png](img/メイン画面.png)

### 8-1. 画面要素

| 領域 | 内容 |
|---|---|
| ヘッダー | 会社名 / 店舗名 / リーダー接続状態 / 管理画面ログインボタン |
| 中央 | 「QRコードをかざしてください」／読み取り窓のビジュアル／現在日時（秒まで更新） |
| 下部 | 店舗からのお知らせ（カード形式、最大5件） |
| フッター | アプリバージョン・端末名 |

現在日時は `clock-display`（64px / tabular numerals）で表示する。秒が動いても桁がぶれないよう等幅数字を使う。

### 8-2. 実装工程

- [x] **8-2-1. シェルと待機画面のレイアウト**
  - `Views/IdleView.xaml` / `ViewModels/IdleViewModel.cs`
  - **完了条件**: 1366x768 と 1920x1080 でスクロールなしに全要素が収まる

- [x] **8-2-2. 時計表示**
  - `DispatcherTimer` で1秒ごとに更新（`IClock` 経由）
  - **完了条件**: 「2026年9月5日 (土曜日) 13:53:17」形式で表示され、曜日が正しい

- [x] **8-2-3. リーダー接続状態の表示**
  - `QrScannerService.ConnectionState` を購読し、色とテキストを切り替える
  - **完了条件**: リーダーを抜くと5秒以内に「未接続」表示に変わり、挿すと自動復帰する
  - **テスト観点**: 実機での抜き差し（実機テスト）

- [x] **8-2-4. お知らせ表示**
  - 店舗設定で登録された項目をカードで表示。0件なら領域ごと非表示
  - **完了条件**: 店舗設定で保存すると、メイン画面へ戻った時点で反映される

- [x] **8-2-5. QR受信 → 打刻画面への遷移**
  - スタッフ解決の結果で分岐（→ 9-1）
  - **完了条件**: 有効QRで打刻画面へ遷移し、無効QRではエラー表示後に待機へ戻る

---

## 9. 打刻処理

参照: [img/メイン画面_打刻.png](img/メイン画面_打刻.png)

### 9-1. QR読取から遷移までの判定

```
QR受信
  │
  ├─ トークン未登録         → エラー「登録されていないQRコードです」→ 3秒後に待機へ
  ├─ トークン失効済み       → エラー「このQRコードは無効です。管理者へ連絡してください」
  ├─ スタッフが休職中       → エラー「休職中のため打刻できません」
  ├─ スタッフが退職済       → エラー「退職済みのため打刻できません」
  └─ 在職中                 → 打刻種別選択画面へ
```

エラー表示は**理由をスタッフに分かる言葉で出す**。ログには詳細（トークンのハッシュ・staff_id）を残す（→ 15章）。

### 9-2. 打刻種別選択画面

- 4種の大型タッチカード（出勤／退勤／中抜け開始／中抜け終了）＋ キャンセル
- 対象スタッフの氏名・社員番号・区分を表示。**前回打刻**（種別と日時）も表示し、誤打刻に気づけるようにする
- **30秒無操作で自動キャンセル**。残り秒数をカウントダウン表示する（モック準拠）
- リーダーが接続断になった場合、**キャンセルのみ有効**にする

### 9-3. 打刻状態遷移

現在の状態は「その勤務日の、そのスタッフの打刻履歴」から導出する（状態列は持たない）。

```
        ┌──────────┐  出勤   ┌──────────┐  退勤   ┌──────────┐
        │  未出勤  │────────▶│  勤務中  │────────▶│  退勤済  │
        └──────────┘         └──────────┘         └──────────┘
                                  │   ▲                 │
                     中抜け開始   │   │ 中抜け終了      │ 出勤（再出勤 → Q8）
                                  ▼   │                 ▼
                              ┌──────────┐          （勤務中へ戻る）
                              │ 中抜け中 │
                              └──────────┘
```

**遷移表**（○=正常 / △=警告して記録 / ×=保存しない）

| 現在状態 \ 操作 | 出勤 | 退勤 | 中抜け開始 | 中抜け終了 |
|---|---|---|---|---|
| 未出勤 | ○ | △ | △ | △ |
| 勤務中 | △ | ○ | ○ | △ |
| 中抜け中 | △ | △ | △ | ○ |
| 退勤済 | △ | △ | △ | △ |

- △ は**警告ダイアログで確認したうえで記録する**（→ Q7）

  **実装時の変更**: 当初は「記録に異常フラグを立てる」としていたが、**列は追加せず打刻列から都度導出する**方式にした。
  管理者が打刻を修正して異常が解消しても、保存したフラグは古いまま残るため。
  勤務状況一覧の「要確認」は `PunchStateMachine` で並びを判定して出す（→ 11-2）。
  `is_outside_business_hours`（営業時間外）は打刻時点の事実で後から変わらないため、これだけは列に持つ
- × は「同一種別を60秒以内に繰り返した場合」のみ。二重打刻として静かに無視し、「すでに打刻済みです」と表示する

### 9-4. 実装工程

- [x] **9-4-1. 打刻状態判定ロジック**
  - `Domain/PunchStateMachine.cs`: 打刻履歴 → 現在状態、状態 × 操作 → 判定結果（正常／警告／重複）
  - **完了条件**: 上記遷移表がすべてテストで網羅されている
  - **テスト観点**: 全16パターン／60秒重複の境界（59秒・61秒）／日をまたぐケース／空履歴

- [x] **9-4-2. 打刻サービス**
  - `Services/PunchService.cs`: 状態判定 → **`BusinessDayResolver` で営業日を決定**（→ 6-7）→ 保存 → 結果を返す
  - 保存は単一トランザクションで即コミットする
  - 営業時間外の打刻には警告フラグを立てる（記録はする）
  - **完了条件**: 打刻がDBに残り、アプリを強制終了しても失われない
  - **テスト観点**: DB書き込み失敗時に「成功扱いにしない」こと／日跨ぎの退勤が出勤と同じ営業日に入ること／営業時間外の打刻が警告になること

- [x] **9-4-3. 打刻種別選択画面**
  - `Views/PunchSelectView.xaml` / `PunchSelectViewModel.cs`
  - **完了条件**: 4ボタン＋キャンセルが動き、リーダー断で4ボタンが無効化される

- [x] **9-4-4. 自動キャンセルタイマー**
  - `App/Services/IdleTimeoutService.cs`: 30秒。任意の操作でリセット。
    共有の `IUiTicker` に相乗りし、画面ごとに1つ持って破棄する（打刻完了の3秒復帰も同じ仕組みを使う）
  - **完了条件**: 無操作30秒でメイン画面へ戻る／操作するとカウントがリセットされる
  - **テスト観点**: タイマーが画面遷移後に確実に停止すること（多重起動によるリークがないこと）

- [x] **9-4-5. 打刻完了表示**
  - `Views/PunchResultView.xaml`: 「出勤を記録しました」＋ 氏名・打刻種別・時刻
  - **3秒後に自動で待機画面へ戻る**
  - **完了条件**: 成功が明確に分かる表示になっている（色・アイコン・文言）

---

## 10. 管理者認証

参照: [img/管理者ログイン.png](img/管理者ログイン.png)

### 10-1. 方式

- 店舗単位の**8桁数字PIN**（複数管理者アカウントは持たない）
- 保存: **PBKDF2-HMAC-SHA256**、ソルト16バイト、反復回数 **210,000**、鍵長32バイト
- 検証は `CryptographicOperations.FixedTimeEquals` で行う（タイミング攻撃対策）

> **PBKDF2 を選ぶ理由**: bcrypt / Argon2 のほうが強いが外部パッケージが要る。.NET 標準の `Rfc2898DeriveBytes` で完結させ、反復回数で強度を確保する。
> **注意**: 8桁数字は1億通りしかなく、ハッシュが流出すればオフライン総当たりは現実的に可能。**この方式は「端末を触れる人の誤操作を防ぐ」ものであり、盗まれたDBを守るものではない**。DBファイル自体の保護（端末の物理管理・ディスク暗号化）が前提になる。

### 10-2. 失敗時の扱い（→ Q10）

1. 失敗するたび `failed_count` を加算
2. 5回連続失敗 → エラーメッセージ表示 → **5分間ロック**（`locked_until` を保存）→ メイン画面へ戻る
3. 成功したら `failed_count` を 0 に戻す
4. ロック中にログイン画面を開いた場合、残り時間を表示して入力させない

### 10-3. 実装工程

- [x] **10-3-1. パスワードハッシュ**
  - `Services/PasswordHasher.cs`: `Hash(pin)` / `Verify(pin, hash, salt, iterations)`
  - **完了条件**: 同じPINから毎回異なるハッシュが生成され、検証は通る
  - **テスト観点**: 正しいPIN／誤ったPIN／ソルトが毎回変わること／反復回数の保存と読み出し

- [x] **10-3-2. 認証サービス**
  - `Services/AdminAuthService.cs`: 認証・失敗カウント・ロック判定
  - **完了条件**: 5回失敗でロックされ、アプリを再起動してもロックが継続する
  - **テスト観点**: 4回失敗→成功でカウントがリセットされること／ロック期限の境界

- [x] **10-3-3. ログイン画面**
  - `Views/AdminLoginView.xaml`、`Controls/NumericKeypad.xaml`（0-9・一文字消去・クリア、各56px以上）
  - 入力は伏せ字。桁数インジケータ（`0 / 8 桁`）を表示
  - 物理キーボードのテンキー入力も受け付ける
  - **完了条件**: タッチのみでログインできる
  - **テスト観点**: 8桁を超えて入力できないこと／タッチ操作（実機テスト）

  **実装時の変更**
  - **店舗の選択欄を置かなかった**。モックにはドロップダウンがあるが、1端末＝1店舗のため
    （→ Q1）。選ばせると誤った店舗にログインできてしまう。店舗名は読み取り専用で表示する
  - **画面は2列に組んだ**。モックは縦1列だが、その構成は 1366x768 に収まらず、
    ロックやエラーの案内が出た瞬間にログインボタンが画面外へ出る
  - **無操作60秒で待機画面へ戻す**（計画に記載なし・要確認 → 下記）
  - 伏せ字の表示切替（モックの目のアイコン）は入れていない。
    桁数インジケータがあれば入力の確認はでき、キオスク端末の画面は誰からも見えるため

  **未実施**: タッチ操作の実機確認（16-3 とあわせて行う）

- [x] **10-3-4. 管理画面シェル**
  - `Views/AdminShellView.xaml`: 勤務状況／スタッフ管理／店舗設定のタブ＋ログアウト
  - 初期タブは**勤務状況**
  - 管理画面滞在中は打刻を受け付けない（QR受信は無視し、ログに残す）
  - **完了条件**: タブ切り替えで各画面が表示され、ログアウトでメイン画面へ戻る

  **各タブの中身は仮表示**（「この画面は plan.md 11章 で作ります」）。
  11〜13章で `AdminTab.CreateContent` を実際の ViewModel に差し替える

---

## 11. 勤務状況管理

参照: [img/管理者_勤務状況.png](img/管理者_勤務状況.png)

### 11-1. 一覧の構造

**1行 = スタッフ1名 × 勤務日1日**。打刻レコードを勤務日で畳んで表示する。

**並び順は画面で切り替える**（2026-09-07 発注者判断）。

- **既定は営業日の降順**（新しい日が上）。開いた直後に見たいのは直近の勤怠だから
- ボタンで昇順へ切り替えられる。月ぶんを通して確認するときに使う
- 同じ営業日のなかは出勤時刻順（降順のときは遅い出勤が上）。
  日付だけで並べると、同じ日の行の順序が取得方法に左右されて安定しない
- **ボタンの文言はいまの並びを表す**（「新しい日付順 ▼」）。
  押した先を表すと、表示中がどちらの順なのか読み取れない
- 切り替えは保存しない。タブを開き直すと既定に戻る

| 列 | 内容 |
|---|---|
| 日付（曜日） | `work_date`（**営業日**。カレンダー日付ではない）。土日は色を変える |
| スタッフ氏名 | |
| 区分 | 社員 / アルバイト / パート |
| 出勤 | その日の最初の `CLOCK_IN` |
| 退勤 | その日の最後の `CLOCK_OUT` |
| 中抜け開始 | 最初の `BREAK_START`（→ Q3） |
| 中抜け終了 | 最後の `BREAK_END` |
| 中抜け合計 | 開始と終了の対応が取れたペアの合計（分） |
| 実労働時間 | (退勤 − 出勤) − 中抜け合計 |
| 状態 | 通常 / 手入力 / 手修正 / **要確認**（異常打刻・未完了） |
| メモ | 修正理由（→ Q20） |
| 操作 | 編集ボタン |

**色分け**（`prompt.txt` の「手入力・手修正は色で判別できる」に対応）

| 状態 | 表示 |
|---|---|
| 通常（QR打刻のみ） | 標準色 |
| 手入力 `MANUAL_ADD` を含む | 濃色バッジ |
| 手修正 `MANUAL_EDIT` を含む | 中間色バッジ |
| 要確認（出勤のみで退勤なし／異常遷移あり） | 赤系バッジ |

### 11-2. 絞り込みと期間

- 月単位（前月／翌月／今月ボタン）
- スタッフ絞り込み（全員／個別）
- 状態絞り込み（すべて／要確認／打刻漏れ）

### 11-3. 手動登録・修正

- **手動登録**: スタッフ・勤務日・打刻種別・時刻・メモを入力して追加。`entry_method = MANUAL_ADD`
- **修正**: 既存レコードの時刻を上書き。`entry_method = MANUAL_EDIT`、メモを更新。**履歴は残さない**（仕様どおり）
- **削除**: 誤登録の取り消しに必要なため実装する（確認ダイアログ必須）
- 登録・修正・削除の後は**一覧を即時再読み込み**する

### 11-4. 実装工程

- [x] **11-4-1. 日次集計ロジック**
  - `Domain/AttendanceAggregator.cs`: 打刻レコード列 → 日次サマリー（出勤・退勤・中抜け・実労働・状態）
  - **完了条件**: 中抜け複数回・退勤なし・出勤なしのすべてで例外を出さずに結果を返す
  - **テスト観点**: 中抜け0/1/複数回／退勤忘れ／中抜け終了忘れ／出勤なしで退勤のみ／日跨ぎ

- [x] **11-4-2. 月次取得サービス**
  - `Services/AttendanceQueryService.cs`: 年月＋絞り込み条件 → 行データ
  - **完了条件**: 1店舗・スタッフ20名・1か月分（約1,200レコード）で 300ms 以内に返る
  - **テスト観点**: 月初・月末の境界／該当なしの場合

- [x] **11-4-3. 勤務状況画面**
  - `Views/AttendanceView.xaml` / `AttendanceViewModel.cs`
  - **完了条件**: 月移動・絞り込み・色分けが動作する

  **モックから外したもの**
  - KPIカード（通常打刻率・手入力件数・店舗稼働日数）→ 1-3 で対象外と判断済み
  - ページ送り → 20名×31日でも約600行。DataGrid の仮想化で足り、
    ページを分けるほうが「先月の◯日を探す」操作を遅くする
  - CSV出力ボタン → 14章で足す

  **追加したもの**: `Resources/Inputs.xaml`。
  既定の WPF の入力欄は旧世代の見た目で、平坦なボタンと並べると壊れて見えるため、
  ComboBox / TextBox のテンプレートを最小限だけ差し替えた（タッチの最小高さもここで担保）

- [x] **11-4-4. 打刻手動登録／修正ダイアログ**
  - `Views/Dialogs/TimeRecordEditDialog.xaml`（中身は `TimeRecordEditPanel.xaml`）
  - 時刻入力はタッチしやすい形式（時・分のスピナーまたはテンキー）
  - **営業日も変更できるようにする。** 自動判定（→ Q2-a）で取りこぼした分を管理者が直せる唯一の手段
  - **完了条件**: 登録・修正・削除が一覧に即時反映される
  - **テスト観点**: 不正な時刻の入力を弾くこと／未来日時の警告／修正後に集計が再計算されること

  **実装時の決定**
  - 時刻は**テンキーで4桁（HHMM）**を組み立てる（10章の `NumericKeypad` を再利用）。
    文字入力にすると「25:70」を受け取ってから弾くことになるが、桁で組み立てれば
    そもそも入力できる範囲を絞れる。24時・60分だけを範囲外として弾く
  - 営業日は**◀ ▶ の日送り**にした。カレンダーは触りにくく、
    実際の用途（自動判定の1日ずれを直す）は前後1日で足りる。
    月をまたいで大きく動かす操作は想定していない
  - 一覧の1行は最大で複数の打刻を含むため、**ダイアログの先頭でその日の打刻を選ばせる**。
    「＋ 新しい打刻を追加」も同じ選択肢に並べ、打刻漏れの補填を同じ画面で行えるようにした
  - **修正では対象スタッフを変更させない。** 変えられると別人の勤怠に付け替わる
  - 中身を `TimeRecordEditPanel`（UserControl）に切り出した。
    Window のままでは表示するまで描画できず、レイアウト崩れを確認できないため

---

## 12. スタッフ管理

参照: [img/管理者_スタッフ管理.png](img/管理者_スタッフ管理.png)

### 12-1. 画面構成

左に名簿（一覧）、右に選択中スタッフの詳細とQRカードプレビュー。

- 一覧列: 社員番号 / 氏名（フリガナ）/ 区分 / 在籍状態 / QR発行状況
- 検索: 氏名・社員番号の部分一致
- 絞り込み: 全員 / 社員 / アルバイト・パート / 退職者
- 操作: 新規登録 / 編集 / 在籍状態変更 / QR発行 / QR再発行 / PNG保存

### 12-2. 在籍状態

| 状態 | 値 | 打刻 |
|---|---|---|
| 在職中 | `ACTIVE` | 可能 |
| 休職中 | `ON_LEAVE` | 不可（エラー表示） |
| 退職済 | `RETIRED` | 不可（エラー表示） |

**物理削除は行わない。** 打刻レコードが参照しているため、削除すると過去の勤務記録が壊れる。

### 12-3. QR発行

- 新規登録時に自動発行
- 再発行時は**旧トークンを失効させてから新規発行**（同一トランザクション）
- 退職済へ変更した時点で有効トークンを失効（→ Q15）

### 12-4. 実装工程

- [x] **12-4-1. スタッフサービス**
  - `Services/StaffService.cs`: 登録・更新・状態変更・一覧取得（検索／絞り込み）
  - **完了条件**: 社員番号の重複が登録できない
  - **テスト観点**: 重複社員番号／空文字の氏名／状態変更に伴うQR失効／カナ検索

- [x] **12-4-2. スタッフ管理画面**
  - `Views/StaffView.xaml` / `StaffViewModel.cs`
  - **完了条件**: 一覧・検索・絞り込み・詳細表示が動作する

  **モックから外したもの**: 顔写真・所属/役職（→ Q6 で将来）、ページ送り、
  「キオスク端末3台へQR暗号キー同期完了」（単一端末のため対象外）

  **実装時の決定**
  - 在籍状態は**選んだ時点で反映**する。「保存」を挟むと、変えたつもりで
    反映されていない状態が生まれる。退職への変更だけ確認ダイアログを挟む
  - 右パネルはスクロールできるようにした。1366x768 の管理画面内で使える高さは
    約 568px しかなく、カードとボタンを並べると収まらない

- [x] **12-4-3. 登録／編集ダイアログ**
  - `Views/Dialogs/StaffEditDialog.xaml`（中身は `StaffEditPanel.xaml`）
  - **完了条件**: 新規登録すると一覧に即時反映され、QRが自動発行される

  区分は3つの選択肢から選ばせる（→ Q6）。ただし**候補に無い値を持つ既存スタッフを
  編集するときは、その値を選択肢へ足す**。足さないと選択が外れ、保存した瞬間に区分が消える

- [x] **12-4-4. QRカード発行UI**
  - プレビュー表示（名刺比率）＋ PNG保存（保存先はダイアログで選択）
  - 再発行は「旧コードが使えなくなる」ことを明示する確認ダイアログを挟む
  - **完了条件**: 保存したPNGを実機リーダーで読み取り、打刻できる
  - **テスト観点**: 再発行後に旧QRで打刻できないこと（結合テスト）

  再発行・退職の両方について、**待機画面で旧QRをかざしても打刻画面へ進まない**ことを
  `ScreenFlowTests` で確認済み。

  モックの「カード印刷（名刺）」は入れていない。計画は
  「プレビュー表示＋PNG保存」までで、印刷は記載が無い

  **未実施**: 保存した PNG を実機リーダーで読み取る確認（16-3 とあわせて行う）

---

## 13. 店舗設定

参照: [img/管理者_店舗設定.png](img/管理者_店舗設定.png)

### 13-1. 設定値の保存先

**すべて SQLite に保存する。**（`device_settings` / `stores` / `announcements`）

> **SQLite を選ぶ理由**
> 1. お知らせ・店舗情報と同じ画面で編集され、**同じトランザクションで整合させたい**
> 2. 将来サーバーへ集約する際、店舗IDに紐づくデータとしてそのまま送れる（設定ファイルだと別経路になる）
> 3. バックアップがDBファイル1つで完結し、「設定だけ古い」という状態が起きない
>
> **例外**: DBファイルの場所とログ出力先は SQLite に書けない（鶏と卵になる）ため、`appsettings.json` に置く。**アプリ設定は「DBを開く前に必要なものだけ」**という基準で分ける。

### 13-2. 画面要素

| 区分 | 項目 | 編集 |
|---|---|---|
| 会社情報 | 会社名 | 不可（表示のみ）|
| 店舗情報 | 店舗ID / 店舗名 / 店舗コード | 店舗名・コードは可 |
| **営業日** | **1日の開始時刻 / 終了時刻（`HH:mm`）** | **可**（→ Q2）|
| お知らせ | 件名（40文字）／項目 最大5件（見出し＋本文）| 可 |
| リーダー設定 | COMポート / BaudRate / DataBits / Parity / StopBits | 可 |
| リーダー操作 | ポート再検出 / 接続 / 切断 / 接続テスト | — |

### 13-3. 接続テストの挙動

1. メインの受信を停止（ポートは排他利用のため）
2. 指定設定でポートを開く
3. 開けなければ理由を表示（使用中／存在しない／権限なし）
4. 開けたら「QRをかざしてください」と促し、**10秒以内に受信があれば成功**とする
5. テスト終了後、元の接続状態へ復帰

### 13-4. 実装工程

- [x] **13-4-1. 設定サービス**
  - `Services/StoreSettingsService.cs`: 店舗情報・お知らせ・デバイス設定の取得と保存
  - **完了条件**: 保存後にメイン画面とリーダー接続へ反映される
  - **テスト観点**: お知らせ5件超過を弾くこと／件名40文字の境界／保存失敗時に画面の値を失わないこと

- [x] **13-4-2. 店舗設定画面**
  - `Views/StoreSettingsView.xaml` / `StoreSettingsViewModel.cs`
  - 「変更を破棄」「設定を保存する」
  - **完了条件**: 未保存のまま他タブへ移動しようとすると確認が出る

  タブ移動の確認は `IConfirmNavigation` として切り出し、シェル側が見る。
  **ログアウトも同じ経路を通す**（未保存のままログアウトしても入力が消える）

  画面は3列（店舗情報・リーダー・お知らせ）。縦に積むと、いちばん使うリーダー設定と
  接続テストの結果が、管理画面内で使える約 568px に収まらない

- [x] **13-4-3. シリアル設定UI**
  - COMポート一覧の再検出、接続／切断、接続テストと結果表示
  - **完了条件**: 実機で接続テストが成功し、結果（応答の有無）が画面に出る
  - **テスト観点**: 存在しないポートを指定した場合／他アプリが掴んでいる場合（実機テスト）

  **接続テストは画面の入力値で行う**（保存済みの設定ではない）。
  保存前に確かめられないと、間違った設定を保存してから気づくことになる。

  再検出しても**保存済みのポートは選択肢に残す**。
  今つながっていないポートを消すと、設定を開いただけで保存済みの値が失われる。

  **未実施**: 実機での接続テスト（存在しないポート／他アプリが掴んでいる場合を含む）。
  16-3 とあわせて行う

---

## 14. CSV出力

### 14-1. 仕様

| 項目 | 値 |
|---|---|
| 文字コード | **UTF-8 BOM付き**（Excel が既定で正しく開くため） |
| 改行 | CRLF |
| 区切り | カンマ |
| 引用 | RFC4180準拠。カンマ・改行・`"` を含む場合のみ `"` で囲み、`"` は `""` にエスケープ |
| ヘッダー行 | あり |
| ファイル名 | `QTimeRecord_勤務状況_{店舗コード}_{yyyyMM}.csv` |
| 保存先 | 保存ダイアログで選択（既定はドキュメント） |
| 出力範囲 | 画面で表示中の年月・絞り込み条件と一致させる |

### 14-2. 列定義

| # | 列名 | 内容 | 未打刻時 |
|---|---|---|---|
| 1 | 店舗ID | UUID | — |
| 2 | 店舗名 | | — |
| 3 | スタッフID | UUID | — |
| 4 | 社員番号 | | 空欄 |
| 5 | スタッフ名 | | — |
| 6 | 区分 | 社員／アルバイト／パート | 空欄 |
| 7 | 日付 | `YYYY-MM-DD`（勤務日） | — |
| 8 | 曜日 | 月〜日 | — |
| 9 | 出勤 | `HH:mm` | 空欄 |
| 10 | 退勤 | `HH:mm` | 空欄 |
| 11 | 中抜け開始 | `HH:mm` | 空欄 |
| 12 | 中抜け終了 | `HH:mm` | 空欄 |
| 13 | 中抜け回数 | 整数（→ Q3） | 0 |
| 14 | 中抜け合計(分) | 整数 | 0 |
| 15 | 実労働時間(分) | 整数 | 空欄 |
| 16 | 打刻区分 | 通常／手入力／手修正 | — |
| 17 | 備考 | 修正理由メモ | 空欄 |

> **時刻を `HH:mm` にする理由**: 秒まで出すと給与計算側で丸め処理が必要になり、目視確認もしづらい。秒が必要になった場合に備え、DBには秒まで保持しておく。
> **実労働時間を「分」で出す理由**: `8.75h` のような小数だと Excel 上で誤差と誤解されやすく、時分形式（`8:45`）だと Excel が時刻型と解釈して集計を誤る。整数の分が最も事故が少ない。

### 14-3. 実装工程

- [x] **14-3-1. CSV生成**
  - `Services/CsvExportService.cs` と `Infrastructure/CsvWriter.cs`
  - **完了条件**: 生成した CSV を Excel で開いて文字化けせず、列がずれない
  - **テスト観点**: 氏名にカンマ／ダブルクォート／改行が含まれる場合／未打刻の空欄／BOMの有無／CRLF／0件の場合（ヘッダーのみ出力）

  出力した CSV を**読み直して列がずれないこと**まで確認する（`Tests/CsvReader.cs`）。
  改行で単純に分割すると、値の中の改行で行が割れても気づけない。

  **未実施**: Excel で開いての確認。実データを含むサンプルを生成して発注者へ渡した

- [x] **14-3-2. 出力UI**
  - 勤務状況画面の「CSVファイル出力」ボタン → 保存ダイアログ → 完了通知
  - **完了条件**: 出力後に保存先フォルダを開けるリンクを表示する
  - **テスト観点**: 保存先が書き込み不可の場合／同名ファイルが存在する場合／出力中の二重実行防止

  同名ファイルの確認は保存ダイアログの `OverwritePrompt` に任せる（OS の標準の挙動）。

  **並び順**: 出力時点の画面の並びに従う（→ 11-1）。
  仕様は出力**範囲**を画面と一致させるとしているが、並びも揃えている。
  画面と CSV で並びが違うと、突き合わせのときに読み違える

---

## 15. エラー処理・ログ

### 15-1. 方針

**利用者向けメッセージと開発・保守用ログを分離する。** 画面には対処が分かる短文を出し、原因の詳細はログに残す。

| 事象 | 画面表示 | ログ |
|---|---|---|
| リーダー未接続 | 「QRリーダーが接続されていません。ケーブルを確認してください」 | ポート名・設定値 |
| 接続断 | 「QRリーダーとの接続が切れました。再接続しています」 | 例外種別・切断時刻 |
| 不正QR（形式違反） | 「読み取れませんでした。もう一度かざしてください」 | 受信バイト長のみ（**内容は残さない**）|
| 未登録QR | 「登録されていないQRコードです」 | トークンのSHA-256先頭8文字 |
| 失効QR | 「このQRコードは無効です。管理者へ連絡してください」 | staff_id・失効日時 |
| 休職スタッフ | 「休職中のため打刻できません」 | staff_id |
| 退職スタッフ | 「退職済みのため打刻できません」 | staff_id |
| DBアクセス失敗 | 「打刻を保存できませんでした。管理者へ連絡してください」 | 例外・SQL状態 |
| CSV出力失敗 | 「CSVを保存できませんでした。保存先を確認してください」 | パス・例外 |
| QR生成失敗 | 「QRコードを生成できませんでした」 | 例外 |
| 認証失敗 | 「パスワードが違います（残り N 回）」 | 失敗回数のみ |
| 予期しない例外 | 「エラーが発生しました。管理者へ連絡してください」＋ログ場所の案内 | 全スタックトレース |

**打刻の失敗を成功に見せない。** 保存に失敗した場合は必ずエラーを表示し、待機画面へ戻す。

### 15-2. 未処理例外の捕捉

- `Application.DispatcherUnhandledException`（UIスレッド）
- `AppDomain.CurrentDomain.UnhandledException`（それ以外）
- `TaskScheduler.UnobservedTaskException`（Task）

UIスレッドの例外は、**打刻中でなければアプリを継続**させる（キオスク用途で落ちると営業に影響するため）。継続不能な場合のみ、ログを残して終了する。

### 15-3. ログ設計

| 項目 | 値 |
|---|---|
| 出力先 | `%LOCALAPPDATA%\QTimeRecord\logs\qtimerecord-YYYYMMDD.log` |
| ローテーション | 日次 |
| 保持世代 | 30日（`retainedFileCountLimit: 30`）|
| 1ファイル上限 | 10MB（超過時は連番）|
| 既定レベル | Information（`appsettings.json` で Debug に変更可）|
| EF Core のログ | **Warning 以上に抑制**（既定では実行した SQL をすべて Information で出すため）|
| 形式 | `[時刻] [レベル] [カテゴリ] メッセージ` |

**ログ対象**

起動 / 終了 / リーダー接続 / 切断 / 再接続 / QR読取（成否）/ 打刻成功 / 打刻失敗 / DBエラー / 管理者ログイン（成否）/ 設定変更 / スタッフ登録・変更 / QR発行・失効 / CSV出力 / 未処理例外

**個人情報の扱い**

- **氏名はログに書かない。** `staff_id` で記録する（ログファイルが流出しても個人が特定されにくい）
- **QRトークンは平文で書かない。** 必要な場合は SHA-256 の先頭8文字のみ
- 管理者PINは一切書かない（入力桁数も残さない）

### 15-4. 実装工程

**15-1 の表との突き合わせ（14章の完了後に実施）**

実装と表を1行ずつ照合し、下記の抜けを埋めた。

| 行 | 見つかった状態 | 対応 |
|---|---|---|
| 接続断 | 赤い点で「リーダー未接続」と出るだけで、理由が伝わらなかった | 待機画面に「接続が切れました。再接続しています」を出す |
| 不正QR（形式違反） | 上限超過の受信を黙って捨てており、**画面にもログにも何も残らなかった** | 受信長だけをログに残し、画面に「読み取れませんでした」を出す |
| 未登録QR / 失効QR / 休職 / 退職 | 画面には出るが**ログに残らなかった**。「打刻できない」の申告を後から追えない | 表のとおり指紋・staff_id・失効日時を残す |
| リーダー未接続 | 「管理者へ連絡してください」だった | 表のとおり「ケーブルを確認してください」に統一 |
| CSV出力失敗 | 例外メッセージを画面に出していた | 対処だけを出し、原因はログへ |
| QR生成失敗 | 「未発行」と同じ表示で、原因が区別できなかった | 「QRコードを生成できませんでした」を出す |
| 予期しない例外 | ログ場所の案内が無かった | メッセージにログ出力先を添える |

**15-2 について**: UI スレッドの例外は常に継続させている。
「打刻中でなければ継続」の趣旨である**打刻の失敗を成功に見せない**ことは、
`PunchService` が失敗を戻り値で返し、`PunchSelectViewModel` が完了画面へ流さないことで担保している。

- [x] **15-4-1. ログ基盤の構築**
  - `Infrastructure/LoggingSetup.cs`（Serilog 設定）／DI へ `ILogger<T>` を登録
  - **完了条件**: 起動・終了がログに出て、翌日になるとファイルが分かれる
  - **テスト観点**: 保持世代を超えた古いファイルが削除されること

- [x] **15-4-2. 例外ハンドラ**
  - 3種のハンドラを `App.xaml.cs` に登録
  - **完了条件**: 意図的に例外を発生させてもアプリが落ちず、ログに残る

- [x] **15-4-3. ユーザー向けエラー表示の共通化**
  - `Services/UserErrorPresenter.cs`: 例外・業務エラー → 表示メッセージへの変換を1か所に集約
  - **完了条件**: 15-1 の表がそのまま実装に対応している
  - **テスト観点**: 想定外の例外でも必ず何らかのメッセージが返ること

  15-1 の全行を実装と照合済み（上表）。
  ログに氏名とトークンが残らないことは `Tests/Infrastructure/LoggedContentTests.cs` で固定した

- [x] **15-4-4. データ保全の確認**
  - WAL・`synchronous=FULL` の設定確認、打刻直後の強制終了テスト
  - **完了条件**: 打刻直後にプロセスを強制終了しても、再起動後にその打刻が残っている
  - 確認済み（9章の完了時）:
    - PRAGMA の適用（`foreign_keys` / `journal_mode=WAL` / `synchronous=FULL`）と、
      コミット済みの行が別接続から即座に見えること — 自動テスト
    - `PunchService` で打刻した直後に `Environment.FailFast` でプロセスを落とし
      （-wal / -shm を残したまま後始末をさせない）、別プロセスで開き直して打刻が
      残っていることを確認 — 手動。手順は 16-3-7 に残す
  - **未確認**: 実機の電源断（OS のキャッシュを含む経路）。導入時に実施する

---

## 16. テスト

### 16-1. 単体テスト（`QTimeRecord.Tests`）

- [x] **16-1-1. 打刻状態判定** — 遷移表の全16パターン／60秒重複の境界／空履歴／日跨ぎ
- [x] **16-1-2. スタッフ状態判定** — 在職中・休職中・退職済の分岐
- [x] **16-1-3. QRトークン** — 生成の一意性／解決（有効・失効・未登録）／再発行での旧失効
- [x] **16-1-4. 受信バッファ** — CR/LF/CRLF／分割受信／連続2件／終端なしタイムアウト／過長データ
- [x] **16-1-5. 日次集計** — 中抜け0/1/複数／退勤忘れ／中抜け終了忘れ／実労働時間の計算
- [x] **16-1-6. CSV生成** — エスケープ（カンマ・引用符・改行）／BOM／CRLF／未打刻の空欄／0件
- [x] **16-1-7. パスワードハッシュ** — 検証の成否／ソルトの変動／反復回数の保持
- [x] **16-1-8. 認証ロック** — 5回失敗でロック／4回失敗→成功でリセット／期限の境界
- [x] **16-1-9. 営業日の算出** — 日中営業／日跨ぎ営業／24時間営業／境界時刻ちょうど／閉店中の未退勤あり・なし／月末をまたぐケース

### 16-2. 結合テスト

DBは一時ファイル（テストごとに破棄）、シリアルは差し替え可能な実装を使う。

画面の並び順まで通す分は `Tests/ScreenFlowTests.cs`、
サービスをまたぐ分は各サービスのテストに置いている。

- [x] **16-2-1. QR読取 → スタッフ取得** — 有効／失効／未登録／休職／退職
- [x] **16-2-2. QR読取 → 打刻保存** — 4種すべてがDBに残ること
- [x] **16-2-3. 管理画面 → 打刻修正 → 一覧反映** — 修正後に集計が再計算されること
- [x] **16-2-4. CSV出力** — 画面の絞り込み条件と出力内容が一致すること
- [x] **16-2-5. QR再発行 → 旧QRで打刻不可**
- [x] **16-2-6. 起動 → マイグレーション → 初回セットアップ**

### 16-3. 実機テスト（チェックリスト）

**手順は [実機テスト手順.md](実機テスト手順.md) に切り出した。** 印刷して現場で使う。
下記は進捗の把握用。**すべて未実施**（QRリーダー・プリンタ・設置端末が要るため）。

- [ ] **16-3-1. QRリーダー実機での読み取り**（発行したPNG・印刷したカードの両方）
- [ ] **16-3-2. シリアル設定**（COMポート検出・BaudRate違いでの失敗確認）
- [ ] **16-3-3. 接続断**（動作中にUSBを抜く → 表示が変わる／打刻ボタンが無効化される）
- [ ] **16-3-4. 再接続**（挿し直すと自動復帰し、打刻が再開できる）
- [ ] **16-3-5. タッチ操作**（全ボタンが指で押せる／誤タップしない間隔である）
- [ ] **16-3-6. 長時間連続稼働**（8時間放置後もメモリが増え続けず、打刻できる）
- [ ] **16-3-7. 電源断**（打刻直後に電源を落とし、復帰後にデータが残っている）
- [ ] **16-3-8. 画面解像度**（1366x768 / 1920x1080）

---

## 17. 実装順序

依存関係の順に並べる。**4→5→6 は前提となるため順守する。**7以降は並行できる。

| 順 | 工程 | 依存 | 目安 |
|---|---|---|---|
| 1 | 4. プロジェクト初期構築 | — | 小 |
| 2 | 5. DB設計・実装 | 4 | 中 |
| 3 | 6. 共通処理 | 4 | 小 |
| 4 | 15. ログ・例外基盤 | 6 | 小 |
| 5 | 7. QR・シリアル通信 | 5, 6 | **大** |
| 6 | 8. メイン画面 | 5, 6, 7 | 中 |
| 7 | 9. 打刻処理 | 5, 7, 8 | 中 |
| 8 | 10. 管理者認証 | 5, 6 | 小 |
| 9 | 12. スタッフ管理 | 5, 7, 10 | 中 |
| 10 | 11. 勤務状況管理 | 5, 10 | **大** |
| 11 | 14. CSV出力 | 11 | 小 |
| 12 | 13. 店舗設定 | 7, 10 | 中 |
| 13 | 16. テスト | 全体 | 中 |

> **7（シリアル通信）を早く着手する理由**: 実機依存で、機種ごとの挙動差（終端文字・応答速度・切断時の例外）が最も読みにくい。ここが後ろにあると、終盤で設計変更を強いられる。**実機を最初に触る。**
>
> **9（打刻処理）より先に 8（メイン画面）を作る理由**: QRを受け取って画面が反応するところまで通すと、以降の動作確認がすべて実機経由でできるようになる。

**最小の縦切り（Walking Skeleton）を最初に作る**ことを推奨する。

```
起動 → DB作成 → シリアル受信 → トークン解決 → 打刻保存 → 完了表示
```

見た目を作り込む前にこの1本を通すと、以降の工程で「保存できない」「読めない」といった根本的な問題に早く気づける。

---

## 18. MVP完成条件

以下がすべて満たされた時点で MVP 完成とする。

**`[x]` は自動テスト・ビルドで確認済み。`[ ]` は実機か発注者の確認が要るもの**
（→ [実機テスト手順.md](実機テスト手順.md)）。

**機能**

- [ ] 実機のQRリーダーでスタッフのQRを読み取り、4種すべての打刻がSQLiteに保存される
- [x] 未登録・失効QR、休職・退職スタッフで打刻が拒否され、理由が画面に出る
- [x] 打刻画面が30秒無操作で自動キャンセルされる
- [x] 打刻完了が明確に表示され、一定時間後に待機画面へ戻る
- [x] 管理者PINでログインでき、5回失敗でロックされる
- [x] 勤務状況タブで当月の全スタッフの打刻状況が一覧でき、手入力・手修正が色で判別できる
- [x] 管理者が打刻を手動登録・修正でき、一覧に即時反映される
- [ ] 当月の勤務状況を UTF-8 BOM付き CSV で出力でき、Excel で文字化けせずに開ける（BOM・CRLF・エスケープは自動テスト済み。**Excel で開く確認は発注者へ依頼中**）
- [x] スタッフの登録・編集・在籍状態変更ができ、物理削除されない
- [x] QRコードを発行・再発行でき、再発行時に旧コードが無効化される
- [ ] QRカードをPNGとして保存でき、その画像を実機で読み取れる
- [x] 店舗からのお知らせを編集でき、メイン画面に反映される
- [x] シリアルポート設定を変更でき、接続テストが実行できる

**非機能**

- [x] インターネット未接続の状態で、上記すべてが動作する（外部通信を行うコードが無い。参照パッケージにも通信するものは含まれない）
- [ ] リーダーを抜いても落ちず、挿し直すと自動復帰する
- [x] 打刻直後にアプリを強制終了しても、そのデータが失われない
- [ ] 8時間の連続稼働でメモリが増え続けない
- [ ] 主要ボタンが 64px 以上で、タッチで誤操作なく押せる（64px は `Size.TouchMin` で担保済み。**指での操作感は実機で確認**）
- [x] 1366x768 と 1920x1080 でレイアウトが破綻しない（両方の解像度で各画面を描画して確認済み。実機の表示は 16-3-8 で確認する）
- [x] 起動・打刻・エラーがログファイルに記録され、日次でローテーションされる

**品質**

- [x] 単体テストが 16-1 の全項目を網羅している
- [x] 結合テストが 16-2 の全項目を通る
- [ ] 実機テスト（16-3）のチェックリストが全項目完了している
- [x] ビルドが警告0で通る
- [x] 配布物に `THIRD-PARTY-NOTICES.txt` が含まれ、依存パッケージの一覧と一致している

---

## 19. 将来拡張時の考慮事項

MVP 実装時点で**構造だけ用意しておく**もの。今回は実装しない。

| 拡張 | 今回やっておくこと |
|---|---|
| 会社サーバーへの送信 | 全テーブルに `store_id`。IDは UUIDv7 で衝突しない。`updated_at` を全テーブルに持ち、差分送信の基準にできる |
| 複数店舗の集中管理 | 店舗をまたぐ集計に耐えるよう、集計ロジックを `store_id` 前提で書く |
| クラウド同期 | 打刻に同期状態列（`sync_status`）を後から追加できるよう、`time_records` を追記のみ（イベント的）に扱う |
| 通常休憩の打刻 | `TimeRecordType` に値を追加するだけで済むよう、種別を文字列で保存し、`switch` の分岐を1か所に集約する |
| 給与計算連携 | CSV の列追加で対応できるよう、CSV生成を集計ロジックから分離する |
| シフト管理 | 「予定」と「実績」の比較が入るため、`time_records` を実績専用として扱い、予定を別テーブルにできる形を保つ |
| Web管理画面 | 業務ロジックを `QTimeRecord.Core` に集約し、WPF に依存させない（Core をそのまま再利用できる） |
| Windows自動起動 | 起動引数でキオスクモードを指定できる形にしておく |
| QRカードのPDF/印刷 | カード画像の生成をサービスに切り出し、出力形式だけ差し替えられるようにする |

### 将来サーバー集約時に効いてくる設計判断（再掲）

- **UUIDv7 の採用** — 店舗ごとに独立採番しても衝突せず、時系列順を保てる
- **オフセット付きISO8601** — 端末のタイムゾーンが変わっても解釈がぶれない
- **`work_date` の保持** — 店舗ごとに営業日の境界が違っても、集約側で再計算せずそのまま束ねられる
- **打刻の追記中心の扱い** — 修正で上書きする仕様のため完全な追記型ではないが、`entry_method` で「QR打刻か人手か」を後から判別できる

---

## 20. 補足：デザイン適用の方針

Google Stitch の HTML は Tailwind 前提で、WPF にそのまま移せない。**見た目を維持しつつ、WPF に適した構造へ置き換える。**

| Stitch側 | WPF での実装 |
|---|---|
| Tailwind ユーティリティクラス | `ResourceDictionary` の `Style` / `Brush`（→ 4-5） |
| CSS Grid の12カラム | `Grid` の `ColumnDefinition`（比率指定） |
| `backdrop-blur` | 使用しない（WPF では高コスト）。半透明の単色で代替 |
| `animate-pulse` | `Storyboard` による `Opacity` アニメーション。ただし常時アニメは長時間稼働で無駄なので、接続状態の変化時のみ |
| スキャンビームのアニメーション | 簡略化。読み取り窓の枠と説明文で代替してよい |
| `hover` 前提の表現 | タッチでは hover が無いため、**押下状態（`IsPressed`）で表現**する |
| 影（多層 box-shadow） | `DropShadowEffect` 1つに集約（多用すると描画負荷が上がる） |

**タッチ設計の必須要件**（DESIGN.md 準拠）

- 主要操作ボタン: 最小 **64px**
- テンキー: 最小 **56px**
- ボタン間隔: 誤タップを避けるため 12px 以上
- 数字表示は等幅（`tnum`）で桁を揃え、秒更新でレイアウトが揺れないようにする
