<UserControl x:Class="QTimeRecord.App.Views.AdminShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:QTimeRecord.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:AdminShellViewModel}"
             Background="{StaticResource Brush.Surface}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- タブとログアウト -->
        <Border Grid.Row="0"
                Background="{StaticResource Brush.SurfaceContainerLowest}"
                BorderBrush="{StaticResource Brush.BorderSubtle}"
                BorderThickness="1"
                CornerRadius="{StaticResource Radius.Card}"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!--
                    タブは ListBox で作る。選択が1つだけという規則を自前で持たずに済み、
                    SelectedItem をそのまま束縛できる。見た目だけを差し替える。
                -->
                <ListBox Grid.Column="0"
                         ItemsSource="{Binding Tabs}"
                         SelectedItem="{Binding SelectedTab, Mode=TwoWay}"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Root"
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="{StaticResource Radius.Button}"
                                                MinHeight="{StaticResource Size.TouchMin}"
                                                MinWidth="180"
                                                Margin="0,0,12,0"
                                                Padding="24,12">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Root" Property="Background"
                                                        Value="{StaticResource Brush.Primary}" />
                                                <Setter Property="Foreground"
                                                        Value="{StaticResource Brush.OnPrimary}" />
                                            </Trigger>
                                            <!-- タッチには hover が無い。押下で手応えを返す -->
                                            <Trigger Property="IsMouseCaptureWithin" Value="True">
                                                <Setter TargetName="Root" Property="Opacity" Value="0.9" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Background" Value="{StaticResource Brush.SurfaceContainerLow}" />
                            <Setter Property="Foreground" Value="{StaticResource Brush.OnSurface}" />
                            <Setter Property="FontFamily" Value="{StaticResource Font.Ui}" />
                            <Setter Property="FontSize" Value="18" />
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Setter Property="Cursor" Value="Hand" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Title}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <Button Grid.Column="1"
                        Content="ログアウト"
                        Style="{StaticResource Button.SecondaryTouch}"
                        Command="{Binding LogoutCommand}"
                        MinWidth="200" />
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Background="{StaticResource Brush.SurfaceContainerLowest}"
                BorderBrush="{StaticResource Brush.BorderSubtle}"
                BorderThickness="1"
                CornerRadius="{StaticResource Radius.Card}"
                Margin="0,16,0,0"
                Padding="24">
            <ContentControl Content="{Binding Content}" />
        </Border>
    </Grid>
</UserControl>
