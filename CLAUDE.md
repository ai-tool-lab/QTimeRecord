# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## このリポジトリの現状

3プロジェクトの実装が進行中。**進捗は [plan.md](plan.md) のチェックボックスが唯一の正**（2026-09-06 時点で 86/101。**残りはすべて実機テストと発注者の確認**）。

| ファイル | 役割 |
|---|---|
| [plan.md](plan.md) | **実装計画かつ進行管理台帳。** 全20章・チェックボックス101項目 |
| [DESIGN.md](DESIGN.md) | Google Stitch 出力のデザイントークン（Precision Kiosk Blue） |
| [img/](img) | 画面モックアップ 6画面（PNG + HTML の対） |
| `prompt.txt` | 発注元の元要件。**リポジトリには含めない**（手元にのみ置く） |
| [実機テスト手順.md](実機テスト手順.md) | 実機でしか確かめられない項目の手順（→ plan.md 16-3） |
| [deploy.md](deploy.md) | 店舗PCへの導入手順。**画面のボタン名を変えたらここも直す** |

**実装タスクを受けたら、まず plan.md の該当工程を読む。** 工程には対象ファイル・完了条件・テスト観点が書いてある。完了したらチェックボックスを `[x]` に更新する。

### 確定済みの前提（2026-09-05 発注者判断）

| | 決定 |
|---|---|
| Q1 | **1端末＝1店舗固定。SQLite は店舗専用**（`stores` は常に1行）。ログイン画面に店舗選択を置かない |
| Q2 | **店舗設定の「1日の開始／終了時刻」で営業日を判定する**（→ 必須ルール1） |
| Q3 | 中抜けが複数回でも1日1行。`中抜け回数` と `中抜け合計(分)` を列で持つ |
| Q4 | お知らせは「件名（40文字）＋ 項目最大5件」。メイン画面ではカード表示 |

| Q2-a | 閉店中の打刻は、**直近の出勤が未退勤かつ18時間以内なら前営業日、それ以外は当日** |

【中】【低】の未確定項目は plan.md 2章を参照する。**推測で実装しない。**

## 資料を読むときの注意

- **`img/` は PNG だけで判断せず、対になる HTML も読む。** HTML にはPNGに写っていない要素が含まれる（社員番号入力モーダル、打刻完了オーバーレイ、ドロップダウンの選択肢など）
- **モックには仕様書に無い機能が多数ある。** 実装前に plan.md 1-3 の対象外リストを確認する。特に「社員番号入力による打刻」は、QR運用の本人確認を無効化するため意図的に対象外にしている

## コマンド

ビルドは警告ゼロを維持する（`TreatWarningsAsErrors`）。

```
dotnet build QTimeRecord.slnx
dotnet run --project QTimeRecord.App
dotnet test QTimeRecord.Tests
dotnet test QTimeRecord.Tests --filter "FullyQualifiedName~PunchState"   # 単体で絞る
dotnet ef migrations add <Name> --project QTimeRecord.Core
```

検証環境には .NET SDK 10.0.400 が入っている。ターゲットは `net10.0-windows`（App）/ `net10.0`（Core・Tests）。

## アーキテクチャ

3プロジェクト構成。**業務ロジックは WPF に依存させない**（将来 Web 管理画面から再利用するため、また単体テストのため）。

```
QTimeRecord.Core/     ドメイン・DB・シリアル通信・ユースケース（WPF非依存）
  Domain/             エンティティ・列挙型・状態遷移・日次集計
  Data/               DbContext・マイグレーション・Repository
  Services/           ユースケース（打刻する / CSVを出す / 認証する）
  Devices/            シリアル通信・QR受信
  Infrastructure/     パス解決・ログ設定・アプリ設定

QTimeRecord.App/      WPF（View / ViewModel / Controls / Resources）
QTimeRecord.Tests/    xUnit
```

Core を Data / Devices に物理分割しないのは、MVP の規模では参照管理コストだけが増えるため。フォルダと名前空間で分ける。

```
 View (XAML) ─Binding─▶ ViewModel ─▶ Service ─▶ Repository ─▶ SQLite
                                     ここに業務ルールを集約

 SerialPort ─▶ ScanBuffer ─▶ QrScannerService ─(event)─▶ ViewModel
              終端で分割      接続管理・再接続
```

インターフェースは**テストで差し替えるものだけ**作る（`ISerialPort` / `IClock` / 各 Repository）。ViewModel ごとのインターフェースは作らない。

## 設計上の必須ルール

素直に実装すると壊れる箇所。コードを書く前に必ず踏まえる。

### 1. 日付は「カレンダー日付」ではなく「営業日」

店舗設定の **1日の開始時刻** で日を切る。開始 11:00 の店舗なら、`09/06 02:00` の退勤は `09/05` の営業日に属する。22:00 出勤 → 翌 02:00 退勤は**同じ1件の勤務**として1行にまとまる。

- 営業日の導出は **`Domain/BusinessDayResolver.cs` に集約する。** 打刻・日次集計・CSV はすべてここを通す
- **`recorded_at` から日付を直接取り出した時点でバグ**（`recorded_at.Date` や `DATE(recorded_at)` を書かない）
- 算出結果は `time_records.work_date` に**保存する**。都度計算にすると、店舗設定を変更した瞬間に確定済みの過去月の勤怠が書き換わる
- 終了時刻は営業日の決定には使わない。**営業時間外（閉店中）かどうかの判定**に使う
- 閉店中の打刻だけは例外で、**直近の出勤が未退勤かつ18時間以内なら前営業日**に付ける。18時間の上限は「退勤忘れ」を「勤務継続中」と誤判定しないためのもので、外すと翌朝の出勤が前日に混ざる

**手入力・手修正も同じ前提に立つ。** 入力欄には「翌日」の切り替えを置き、
`recorded_at` は **暦日**（営業日＋必要なら1日）で組み立てる。`work_date + 入力時刻` にすると、
営業日 09/08 に入れた退勤 02:00 が出勤の10時間半前になり、実労働が計算できなくなる（2026-09-09 の不具合）。
保存される暦日は画面に出す。営業日の表示だけでは、どちらの日に入るのか管理者に読み取れない。

**0時間と「計算できない」を混ぜない。** 退勤が出勤より前など打刻が壊れている日は、
実労働を `0` に丸めず**未計算のまま出す**（一覧では `—`）。0h と表示すると、
「0時間働いた」ようにしか見えず、打刻が壊れていることが誰にも伝わらない。

### 2. 打刻をブロックしない

「出勤 → 出勤」のような異常な遷移でも、**警告したうえで記録は残す**。現場で打刻できないことの損害のほうが大きく、管理者が後から修正できる設計になっている。保存しないのは「同一種別を60秒以内に繰り返した場合」（連打・二度読み）のみ。

### 3. 打刻の保存失敗を成功に見せない

DB書き込みに失敗したら必ずエラーを表示して待機画面へ戻す。打刻は勤怠の証跡であり、静かに失われてはいけない。SQLite は `journal_mode=WAL` / `synchronous=FULL` で開き、打刻ごとに即コミットする。

### 4. シリアルポートは排他利用

店舗設定の「接続テスト」を実行する間は、メインの受信を停止してから開き、終了後に復帰させる。守らないと `UnauthorizedAccessException` が出る。接続断の検知は `ErrorReceived` / 読み取り例外 / `GetPortNames()` のポーリングを併用する（USB 抜去はポーリングでないと拾えない）。

### 5. 設定は SQLite に置く

シリアル設定・お知らせ・店舗情報はすべて DB。`appsettings.json` に置くのは**DBを開く前に必要なものだけ**（DBファイルパス・ログ出力先・ログレベル）。この基準を崩すと「設定だけ古い」状態が生まれる。

### 6. ログに個人情報とQRトークンを書かない

氏名ではなく `staff_id` を記録する。QRトークンは平文で残さず、必要なら SHA-256 の先頭8文字のみ（`LogSafe.TokenFingerprint`）。管理者PINは桁数も残さない。

**残らないことはテストで押さえる**（`Tests/Infrastructure/LoggedContentTests.cs`）。
氏名やトークンが混ざっても動作は変わらないため、書いた本人以外は気づけない。

裏返して、**失敗は必ずログに残す**。「かざしても打刻できない」という申告に対し、
何が起きたのかを後から確かめる手段が無くなる。

### 7. スタッフを物理削除しない

在籍状態（`ACTIVE` / `ON_LEAVE` / `RETIRED`）で論理管理する。打刻レコードが参照しているため、削除すると過去の勤務記録が壊れる。

### 8. 時刻は `IClock` 経由で取る

`DateTime.Now` を直接呼ばない。固定時刻で状態遷移・集計・日跨ぎをテストできなくなる。

### 9. ID は UUIDv7、日時はオフセット付き ISO8601

`Guid.CreateVersion7()`。将来サーバーへ集約したとき店舗をまたいで衝突せず、時系列順でインデックスが断片化しない。日時は `2026-09-05T13:53:17+09:00` 形式の `TEXT`。人が読む「店舗コード」「社員番号」は主キーにせず別列で持つ。

### 11. 画面の遷移順を知るのは `ScreenFlow` だけ

ViewModel は「終わった」ことをイベントで伝えるだけにし、次の画面を自分で作らない。
各画面が次を知ると、画面どうしが互いを参照し合い、「どこから来たのか」を全画面が持つことになる。

`NavigationService.NavigateTo` は**前の画面が `IDisposable` なら破棄する**。
購読やタイマーはここで切れる前提なので、ViewModel の `Dispose` は必ず購読解除まで書く。

### 12. 無操作タイマーは共有ティッカーに相乗りする

`IdleTimeoutService` を画面ごとに1つ持ち、画面を離れるときに破棄する（DI は `Transient`）。
`DispatcherTimer` を画面ごとに持つと止め忘れが起き、裏で動き続けたタイマーが次の画面を巻き戻す。

### 13. 日次集計も保存しない

「1行＝スタッフ×営業日」の集計（`AttendanceAggregator`）は打刻から都度導出する。
保存すると、管理者が打刻を直したときに集計だけ古いまま残る。

異常（要確認）の判定は **`PunchStateMachine` を通す**。ここで別の規則を書くと、
打刻時に警告した内容と一覧の「要確認」がずれる。

**理由を持たない「要確認」を作らない。** `DailyAttendance.ReviewReasons` に理由を入れ、
`NeedsReview` はそこから導く。一覧に出るのは最初の出勤と最後の退勤だけなので、
中抜けの閉じ忘れ（中抜け列は `—` のまま）や出勤の重複は列に現れず、
バッジだけでは管理者がどの打刻を直せばよいのか分からない（2026-09-09 の申告）。

**実労働の長さは要確認の理由にしない。** 12時間勤務も夜勤も打刻としては正常で、
長さの妥当性は勤怠の締めで見るもの。ここで警告すると夜勤の店では全行が赤くなる。

### 14. 画面のリソースは `Resources/ViewMap.xaml` に置く

変換器と「ViewModel → View」の DataTemplate はここにまとめ、App.xaml からは読み込むだけにする。
目視確認用の描画（`Tests/ViewRenderer`）が同じ辞書を読むため、
App.xaml に直接書くと、確認した見た目と本番の見た目がずれる。

### 15. MVP 対象外を実装しない

会社サーバー通信 / クラウド同期 / 複数店舗の集中管理 / 給与計算 / シフト管理 / 通常休憩の打刻 / 休憩自動控除 / Web管理画面 / スマホアプリ。

ただし将来拡張しやすいよう、**全テーブルに `store_id` と `updated_at` を持たせる**（差分送信の基準になる）。

## デザインの適用方針

DESIGN.md のトークンを WPF の `ResourceDictionary` に写し、**色・文字サイズを XAML にハードコードしない**。

Stitch の HTML は Tailwind 前提でそのままは移せない。見た目を維持しつつ WPF に適した構造へ置き換える（対応表は plan.md 20章）。

- **タッチ最小サイズ**: 主要ボタン 64px / テンキー 56px / ボタン間隔 12px 以上
- **hover 前提の表現は使わない。** タッチには hover が無いため、押下状態（`IsPressed`）で表現する
- 数字は等幅（`tnum`）で桁を揃える。時計の秒更新でレイアウトが揺れないようにするため
- `backdrop-blur` や多層シャドウは使わない（WPF では描画負荷が高い）
- 入力欄（ComboBox / TextBox）は `Resources/Inputs.xaml` のテンプレートを使う。
  既定の WPF テンプレートは旧世代の見た目で、平坦なボタンと並べると壊れて見える

## 用語

| 用語 | 内部値 | 補足 |
|---|---|---|
| 出勤 / 退勤 | `CLOCK_IN` / `CLOCK_OUT` | |
| 中抜け開始 / 中抜け終了 | `BREAK_START` / `BREAK_END` | 通常の休憩は打刻対象外（給与側で自動控除される前提） |
| 在籍状態 | `ACTIVE` / `ON_LEAVE` / `RETIRED` | 休職中・退職済は打刻不可 |
| 登録方法 | `QR` / `MANUAL_ADD` / `MANUAL_EDIT` | 一覧で色分けする根拠。手入力・手修正が判別できることは要件 |
| 営業日 | `work_date` | 店舗設定の開始時刻で切った日。カレンダー日付ではない。ルール1参照 |
